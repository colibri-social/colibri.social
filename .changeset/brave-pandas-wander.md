---
"@colibri-social/client": patch
---

Fixes two crashes. Stopping a native screen share no longer errors out when the capture ends by itself at the same moment, and a long press on mobile can no longer crash the app when the screen holding the pressed item goes away while your finger is still down.
