---
"@colibri-social/client": minor
---

Adds a debug information section to the About settings page, with a button to copy everything (app/build, device, account, and runtime state) as a paste-ready block. Web builds now also report a real client version and commit instead of just "Web".
