---
"@colibri-social/client": patch
---

Show the channel loading states ("Loading messages", "Loading older messages", "Catching up") as a floating pill over the message list instead of an in-flow line at the top, so the conversation no longer shifts when they appear or disappear. The pill is now shared with the reconnecting indicator.
