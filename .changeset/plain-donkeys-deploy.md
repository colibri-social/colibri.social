---
"@colibri-social/wrapper": patch
---

Adds a Google Play production release path to CI.

The client and wrapper packages are now a fixed version group, so they always release under the same version.
