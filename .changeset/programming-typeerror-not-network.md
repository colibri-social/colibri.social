---
"@colibri-social/client": patch
---

Reports a TypeError from a code bug, such as reading a property of `undefined`, as an unexpected error instead of "The connection dropped", and logs the failing method and cause for uncaught render errors
