---
"@colibri-social/wrapper": patch
---

Adds the new Liquid Glass app icon for macOS and iOS, built from an Icon Composer source so it renders with the real adaptive glass effect on macOS 26+ and iOS 26+ (with a flattened fallback on older systems).

<!-- whatsnew
title: New app icon on macOS
icon: sparkle-fill
body: Colibri has a new Liquid Glass app icon on macOS and iOS.
kind: feature
-->
