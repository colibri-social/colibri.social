name: Publish

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to build (e.g. 0.1.0-rc.3)."
        required: true
      appstore:
        description: "Also build and upload the Apple App Store packages."
        type: boolean
        default: true
      appstore_only:
        description: "Only build and upload the Apple App Store packages, skipping every other channel."
        type: boolean
        default: false
      appstore_platform:
        description: "Which Apple App Store packages to build."
        type: choice
        default: both
        options:
          - both
          - macos
          - ios
      play_track:
        description: "Google Play track to publish to."
        type: choice
        default: production
        options:
          - production
          - alpha
          - beta
      play_mirror_testing:
        description: "Also assign the build to the alpha and beta testing tracks."
        type: boolean
        default: true

concurrency:
  group: publish-${{ inputs.version }}-${{ inputs.appstore_only }}-${{ inputs.appstore_platform }}-${{ inputs.play_track }}
  cancel-in-progress: false

jobs:
  publish-tauri:
    name: Publish (${{ matrix.label }})
    if: ${{ !inputs.appstore_only }}
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: "macos-latest"
            label: "macOS"
            args: "--target universal-apple-darwin"
          - platform: "ubuntu-24.04"
            label: "Linux x64"
            args: ""
            sentry-release: true
          - platform: "ubuntu-24.04-arm"
            label: "Linux arm64"
            args: ""
          - platform: "windows-latest"
            label: "Windows"
            args: "--bundles nsis"
          # Android configuration targeting ubuntu-latest
          - platform: "ubuntu-latest"
            label: "Android"
            mobile: "android"
            is-android: true

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v7

      - name: setup pnpm
        uses: pnpm/action-setup@v6

      - name: setup node
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: "pnpm"

      - name: install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache twemoji assets
        uses: actions/cache@v4
        with:
          path: packages/assets/files/twemoji
          key: twemoji-${{ runner.os }}-${{ hashFiles('packages/assets/scripts/fetch-emoji-assets.mjs', 'packages/client/package.json') }}
          restore-keys: twemoji-${{ runner.os }}-

      - name: Fetch twemoji assets
        run: pnpm --filter @colibri-social/assets fetch-emoji

      - name: Build frontend dependencies
        run: pnpm --filter "@colibri-social/wrapper^..." build

      - name: install dependencies (ubuntu desktop only)
        if: (matrix.platform == 'ubuntu-24.04' || matrix.platform == 'ubuntu-24.04-arm') && !matrix.is-android
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf xdg-utils

      # Android Prerequisites: JDK & Android NDK/SDK configuration
      - name: set up JDK 17 (Android only)
        if: matrix.is-android
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"

      - name: setup Android SDK (Android only)
        if: matrix.is-android
        uses: android-actions/setup-android@v3

      - name: Verify prebuilt Assets.car (macOS only)
        if: matrix.platform == 'macos-latest'
        run: node packages/wrapper/scripts/assets-car.mjs --check

      - name: Select Xcode 26.6 (macOS only)
        if: matrix.platform == 'macos-latest'
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.6"

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: |
            ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}
            ${{ matrix.is-android && 'aarch64-linux-android,armv7-linux-androideabi,i686-linux-android,x86_64-linux-android' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "packages/wrapper/src-tauri -> target"
          shared-key: tauri-${{ matrix.platform }}${{ matrix.is-android && '-android' || '' }}
          cache-on-failure: true
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: install App Store Connect API key (macOS only)
        if: matrix.platform == 'macos-latest'
        env:
          APPLE_API_KEY_P8: ${{ secrets.APPLE_API_KEY_P8 }}
        run: |
          mkdir -p ~/.appstoreconnect/private_keys
          echo "$APPLE_API_KEY_P8" > ~/.appstoreconnect/private_keys/AuthKey.p8
          echo "APPLE_API_KEY_PATH=$HOME/.appstoreconnect/private_keys/AuthKey.p8" >> "$GITHUB_ENV"

      # Android Code Signing setup matching official docs
      - name: Setup Android signing (Android only)
        if: matrix.is-android
        env:
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
          ANDROID_KEY_BASE64: ${{ secrets.ANDROID_KEY_BASE64 }}
        run: |
          mkdir -p packages/wrapper/src-tauri/gen/android
          cd packages/wrapper/src-tauri/gen/android
          echo "keyAlias=${{ env.ANDROID_KEY_ALIAS }}" > keystore.properties
          echo "password=${{ env.ANDROID_KEY_PASSWORD }}" >> keystore.properties
          mkdir -p $RUNNER_TEMP/keystore
          echo "$ANDROID_KEY_BASE64" | base64 --decode > $RUNNER_TEMP/keystore/release.jks
          echo "storeFile=$RUNNER_TEMP/keystore/release.jks" >> keystore.properties

      # openssl-sys builds vendored OpenSSL from source for the Android targets
      # (no prebuilt ships for them)
      - name: Alias NDK binutils for openssl-src (Android only)
        if: matrix.is-android
        shell: bash
        run: |
          set -eu
          NDK="${ANDROID_NDK_ROOT:-${ANDROID_NDK_LATEST_HOME:-${NDK_HOME:-}}}"
          if [ -z "$NDK" ]; then
            echo "No Android NDK path found (ANDROID_NDK_ROOT/ANDROID_NDK_LATEST_HOME/NDK_HOME)" >&2
            exit 1
          fi
          BIN=$(echo "$NDK"/toolchains/llvm/prebuilt/*/bin)
          echo "Using NDK llvm bin dir: $BIN"
          test -x "$BIN/llvm-ranlib"
          for prefix in aarch64-linux-android arm-linux-androideabi armv7a-linux-androideabi i686-linux-android x86_64-linux-android; do
            ln -sf "$BIN/llvm-ranlib" "$BIN/${prefix}-ranlib"
            ln -sf "$BIN/llvm-ar" "$BIN/${prefix}-ar"
          done
          ls -l "$BIN"/*-ranlib
          # OpenSSL's Makefile calls ranlib by bare name, so the aliases must be
          # on PATH when tauri-action runs cargo build in the following step.
          echo "$BIN" >> "$GITHUB_PATH"

      - name: Sync version
        shell: bash
        env:
          VERSION: ${{ inputs.version }}
          IS_ANDROID: ${{ matrix.is-android }}
        run: |
          set -euo pipefail
          SYNC_VERSION="$VERSION"
          if [ "${IS_ANDROID:-}" = "true" ]; then
            SYNC_VERSION=$(node packages/wrapper/scripts/appstore-version.mjs "$VERSION")
          fi
          node packages/wrapper/scripts/set-version.mjs "$SYNC_VERSION" "$GITHUB_RUN_NUMBER"

      - name: Compute Android version code (Android only)
        if: matrix.is-android
        run: echo "ANDROID_VERSION_CODE=$((1000 + GITHUB_RUN_NUMBER))" >> "$GITHUB_ENV"

      - uses: tauri-apps/tauri-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_UPDATER_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_UPDATER_PASSWORD }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_RELEASE_UPLOAD: ${{ matrix.sentry-release && '1' || '' }}
          VITE_SENTRY_DSN: ${{ secrets.WRAPPER_SENTRY_DSN }}
          SENTRY_DSN: ${{ secrets.NATIVE_SENTRY_DSN }}
          # macOS Environment Variables
          APPLE_CERTIFICATE: ${{ matrix.platform == 'macos-latest' && secrets.APPLE_CERTIFICATE || '' }}
          APPLE_CERTIFICATE_PASSWORD: ${{ matrix.platform == 'macos-latest' && secrets.APPLE_CERTIFICATE_PASSWORD || '' }}
          APPLE_SIGNING_IDENTITY: ${{ matrix.platform == 'macos-latest' && secrets.APPLE_SIGNING_IDENTITY || '' }}
          APPLE_API_ISSUER: ${{ matrix.platform == 'macos-latest' && secrets.APPLE_API_ISSUER || '' }}
          APPLE_API_KEY: ${{ matrix.platform == 'macos-latest' && secrets.APPLE_API_KEY || '' }}
          APPLE_API_KEY_PATH: ${{ env.APPLE_API_KEY_PATH }}
          ANDROID_KEY_ALIAS: ${{ matrix.platform == 'ubuntu-latest' && secrets.ANDROID_KEY_ALIAS || '' }}
          ANDROID_KEY_BASE64: ${{ matrix.platform == 'ubuntu-latest' && secrets.ANDROID_KEY_BASE64 || '' }}
          ANDROID_KEY_PASSWORD: ${{ matrix.platform == 'ubuntu-latest' && env.ANDROID_KEY_PASSWORD || '' }}
          ANDROID_VERSION_CODE: ${{ env.ANDROID_VERSION_CODE }}
        with:
          projectPath: "packages/wrapper"
          tagName: v${{ inputs.version }}
          releaseName: "Colibri Social ${{ inputs.version }}"
          releaseBody: ""
          releaseDraft: false
          prerelease: true
          args: ${{ matrix.args }}
          mobile: ${{ matrix.mobile }}

      - name: Verify macOS signing and notarization
        if: matrix.platform == 'macos-latest'
        run: |
          set -euo pipefail
          DMG=$(ls packages/wrapper/src-tauri/target/universal-apple-darwin/release/bundle/dmg/*.dmg | head -1)
          hdiutil attach -nobrowse -readonly "$DMG" -mountpoint /tmp/colibri-verify
          trap 'hdiutil detach /tmp/colibri-verify || true' EXIT
          APP=$(ls -d /tmp/colibri-verify/*.app | head -1)
          codesign --verify --deep --strict --verbose=2 "$APP"
          xcrun stapler validate "$APP"
          spctl --assess --type exec --verbose=4 "$APP"

      - name: Upload Rust debug symbols to Sentry
        shell: bash
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: colibri-social
          SENTRY_PROJECT: rust-native-app
        run: |
          if [ -z "${SENTRY_AUTH_TOKEN:-}" ]; then
            echo "SENTRY_AUTH_TOKEN is not set, skipping debug symbol upload."
            exit 0
          fi
          pnpm dlx @sentry/cli debug-files upload --include-sources packages/wrapper/src-tauri/target

      - name: Route AAB to Play, remove from release (Android only)
        if: matrix.is-android
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          TAG="v$VERSION"
          REPO="${{ github.repository }}"
          AAB=$(gh release view "$TAG" --repo "$REPO" --json assets --jq '[.assets[].name | select(test("\\.aab$"))][0] // ""')
          test -n "$AAB"
          gh release download "$TAG" --repo "$REPO" --pattern "$AAB" --dir aab-out
          gh release delete-asset "$TAG" "$AAB" --yes

      - name: Upload AAB artifact (Android only)
        if: matrix.is-android
        uses: actions/upload-artifact@v4
        with:
          name: android-aab
          path: aab-out/*.aab
          if-no-files-found: error

  mas:
    name: Mac App Store
    if: ${{ (inputs.appstore || inputs.appstore_only) && inputs.appstore_platform != 'ios' }}
    runs-on: macos-latest
    permissions:
      contents: read
    env:
      APPLE_APP_SIGNING_IDENTITY: "Apple Distribution: Louis Escher (8V8SWK3942)"
      APPLE_INSTALLER_SIGNING_IDENTITY: "3rd Party Mac Developer Installer: Louis Escher (8V8SWK3942)"
      PKG_PATH: "packages/wrapper/src-tauri/Colibri Social (App Store).pkg"
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: setup pnpm
        uses: pnpm/action-setup@v6

      - name: setup node
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: "pnpm"

      - name: install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache twemoji assets
        uses: actions/cache@v4
        with:
          path: packages/assets/files/twemoji
          key: twemoji-${{ runner.os }}-${{ hashFiles('packages/assets/scripts/fetch-emoji-assets.mjs', 'packages/client/package.json') }}
          restore-keys: twemoji-${{ runner.os }}-

      - name: Fetch twemoji assets
        run: pnpm --filter @colibri-social/assets fetch-emoji

      - name: Build frontend dependencies
        run: pnpm --filter "@colibri-social/wrapper^..." build

      - name: Verify prebuilt Assets.car
        run: node packages/wrapper/scripts/assets-car.mjs --check

      - name: Select Xcode 26.6
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.6"

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "packages/wrapper/src-tauri -> target"
          shared-key: mas-macos
          cache-on-failure: true
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: install Apple provisioning profile
        env:
          MAC_PROVISIONPROFILE: ${{ secrets.MAC_PROVISIONPROFILE }}
        run: |
          set -euo pipefail
          echo "$MAC_PROVISIONPROFILE" | base64 --decode > packages/wrapper/src-tauri/Colibri_Mac.provisionprofile
          test -s packages/wrapper/src-tauri/Colibri_Mac.provisionprofile

      - name: import signing certificates
        env:
          APPLE_DISTRIBUTION_CERTIFICATE: ${{ secrets.IOS_CERTIFICATE }}
          APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD: ${{ secrets.IOS_CERTIFICATE_PASSWORD }}
          MAC_INSTALLER_CERTIFICATE: ${{ secrets.MAC_INSTALLER_CERTIFICATE }}
          MAC_INSTALLER_CERTIFICATE_PASSWORD: ${{ secrets.MAC_INSTALLER_CERTIFICATE_PASSWORD }}
        run: |
          set -euo pipefail
          KEYCHAIN="$RUNNER_TEMP/colibri-mas.keychain-db"
          KEYCHAIN_PASSWORD=$(uuidgen)
          echo "MAS_KEYCHAIN=$KEYCHAIN" >> "$GITHUB_ENV"

          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"

          echo "$APPLE_DISTRIBUTION_CERTIFICATE" | base64 --decode > "$RUNNER_TEMP/distribution.p12"
          echo "$MAC_INSTALLER_CERTIFICATE" | base64 --decode > "$RUNNER_TEMP/installer.p12"

          security import "$RUNNER_TEMP/distribution.p12" -k "$KEYCHAIN" \
            -P "$APPLE_DISTRIBUTION_CERTIFICATE_PASSWORD" -A
          security import "$RUNNER_TEMP/installer.p12" -k "$KEYCHAIN" \
            -P "$MAC_INSTALLER_CERTIFICATE_PASSWORD" -A
          rm -f "$RUNNER_TEMP/distribution.p12" "$RUNNER_TEMP/installer.p12"

          security set-key-partition-list -S apple-tool:,apple:,codesign: \
            -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN" >/dev/null

          EXISTING=$(security list-keychains -d user | tr -d '"' | tr '\n' ' ')
          security list-keychains -d user -s "$KEYCHAIN" $EXISTING
          security default-keychain -s "$KEYCHAIN"

          security find-identity -v "$KEYCHAIN"
          security find-identity -v "$KEYCHAIN" | grep -qF "$APPLE_APP_SIGNING_IDENTITY"
          security find-identity -v "$KEYCHAIN" | grep -qF "$APPLE_INSTALLER_SIGNING_IDENTITY"

      - name: install App Store Connect API key
        env:
          APPLE_API_KEY_P8: ${{ secrets.APPLE_API_KEY_P8 }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
        run: |
          set -euo pipefail
          mkdir -p ~/.appstoreconnect/private_keys
          echo "$APPLE_API_KEY_P8" > "$HOME/.appstoreconnect/private_keys/AuthKey_${APPLE_API_KEY}.p8"

      - name: Sync App Store version
        run: |
          set -euo pipefail
          MAS_VERSION=$(node packages/wrapper/scripts/appstore-version.mjs "${{ inputs.version }}")
          echo "App Store marketing version: $MAS_VERSION (build $GITHUB_RUN_NUMBER)"
          node packages/wrapper/scripts/set-version.mjs "$MAS_VERSION" --bundle-version="$GITHUB_RUN_NUMBER"
          echo "MAS_VERSION=$MAS_VERSION" >> "$GITHUB_ENV"

      - name: Build and sign App Store package
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_RELEASE_UPLOAD: "1"
          VITE_SENTRY_DSN: ${{ secrets.WRAPPER_SENTRY_DSN }}
          SENTRY_DSN: ${{ secrets.NATIVE_SENTRY_DSN }}
        run: pnpm --filter @colibri-social/wrapper build:macos:appstore

      - name: Validate package with App Store Connect
        id: validate
        env:
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
        run: |
          set -uo pipefail
          LOG="$RUNNER_TEMP/altool-validate.log"
          if xcrun altool --validate-app -f "$PKG_PATH" -t macos \
            --apiKey "$APPLE_API_KEY" --apiIssuer "$APPLE_API_ISSUER" 2>&1 | tee "$LOG"; then
            echo "validated=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          CODES=$(grep -oE '\(9[0-9]{4}\)' "$LOG" | tr -d '()' | sort -u)
          echo "App Store Connect error codes: ${CODES:-none}"
          if [ -n "$CODES" ] && [ -z "$(echo "$CODES" | grep -vE '^(90186|90062)$')" ]; then
            echo "validated=false" >> "$GITHUB_OUTPUT"
            echo "::warning title=App Store upload skipped::Version $MAS_VERSION is already approved or its pre-release train is closed, so App Store Connect will not accept more builds for it. The signed package is still attached as an artifact. Ship the next App Store build by cutting a release whose derived version is higher."
            exit 0
          fi
          exit 1

      - name: Render App Store release notes
        if: steps.validate.outputs.validated == 'true'
        run: |
          set -euo pipefail
          NOTES_VERSION=$(node -p "require('./packages/client/package.json').version")
          node --experimental-strip-types scripts/render-release-notes.ts \
            --version="$NOTES_VERSION" --platform=macos --max=4000 \
            --out="$RUNNER_TEMP/whats-new.txt"
          cat "$RUNNER_TEMP/whats-new.txt"

      - name: Set up Python
        if: steps.validate.outputs.validated == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install App Store Connect CLI
        if: steps.validate.outputs.validated == 'true'
        run: pip install --disable-pip-version-check codemagic-cli-tools==0.69.0

      - name: Upload and submit to App Store
        if: steps.validate.outputs.validated == 'true'
        env:
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APPLE_API_ISSUER }}
          APP_STORE_CONNECT_KEY_IDENTIFIER: ${{ secrets.APPLE_API_KEY }}
        run: |
          set -euo pipefail
          app-store-connect publish \
            --path "$PKG_PATH" \
            --app-store \
            --platform MAC_OS \
            --version-string "$MAS_VERSION" \
            --release-type AFTER_APPROVAL \
            --cancel-previous-submissions \
            --whats-new "@file:$RUNNER_TEMP/whats-new.txt" \
            --max-build-processing-wait 45

      - name: Upload package artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: macos-appstore-pkg
          path: packages/wrapper/src-tauri/*.pkg
          if-no-files-found: warn

      - name: Clean up keychain
        if: always()
        run: |
          if [ -n "${MAS_KEYCHAIN:-}" ] && [ -f "$MAS_KEYCHAIN" ]; then
            security delete-keychain "$MAS_KEYCHAIN" || true
          fi

  ios:
    name: iOS App Store
    if: ${{ (inputs.appstore || inputs.appstore_only) && inputs.appstore_platform != 'macos' }}
    runs-on: macos-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: setup pnpm
        uses: pnpm/action-setup@v6

      - name: setup node
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: "pnpm"

      - name: install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache twemoji assets
        uses: actions/cache@v4
        with:
          path: packages/assets/files/twemoji
          key: twemoji-${{ runner.os }}-${{ hashFiles('packages/assets/scripts/fetch-emoji-assets.mjs', 'packages/client/package.json') }}
          restore-keys: twemoji-${{ runner.os }}-

      - name: Fetch twemoji assets
        run: pnpm --filter @colibri-social/assets fetch-emoji

      - name: Build frontend dependencies
        run: pnpm --filter "@colibri-social/wrapper^..." build

      - name: Select Xcode 26.6
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.6"

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "packages/wrapper/src-tauri -> target"
          shared-key: ios
          cache-on-failure: true
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: install App Store Connect API key
        env:
          APPLE_API_KEY_P8: ${{ secrets.APPLE_API_KEY_P8 }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
        run: |
          set -euo pipefail
          mkdir -p ~/.appstoreconnect/private_keys
          KEY_PATH="$HOME/.appstoreconnect/private_keys/AuthKey_${APPLE_API_KEY}.p8"
          echo "$APPLE_API_KEY_P8" > "$KEY_PATH"
          echo "APPLE_API_KEY_PATH=$KEY_PATH" >> "$GITHUB_ENV"

      - name: Sync App Store version
        run: |
          set -euo pipefail
          IOS_VERSION=$(node packages/wrapper/scripts/appstore-version.mjs "${{ inputs.version }}")
          echo "App Store marketing version: $IOS_VERSION (build $GITHUB_RUN_NUMBER)"
          node packages/wrapper/scripts/set-version.mjs "$IOS_VERSION" --bundle-version="$GITHUB_RUN_NUMBER"
          echo "IOS_VERSION=$IOS_VERSION" >> "$GITHUB_ENV"

      - name: Build and sign App Store IPA
        env:
          IOS_CERTIFICATE: ${{ secrets.IOS_CERTIFICATE }}
          IOS_CERTIFICATE_PASSWORD: ${{ secrets.IOS_CERTIFICATE_PASSWORD }}
          IOS_MOBILE_PROVISION: ${{ secrets.IOS_PROVISIONPROFILE }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_DEVELOPMENT_TEAM: "8V8SWK3942"
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          VITE_SENTRY_DSN: ${{ secrets.WRAPPER_SENTRY_DSN }}
          SENTRY_DSN: ${{ secrets.NATIVE_SENTRY_DSN }}
        run: pnpm --filter @colibri-social/wrapper build:ios:appstore

      - name: Locate IPA
        run: |
          set -euo pipefail
          IPA=$(ls packages/wrapper/src-tauri/gen/apple/build/*/*.ipa | head -1)
          test -s "$IPA"
          echo "IPA_PATH=$IPA" >> "$GITHUB_ENV"

      - name: Validate IPA with App Store Connect
        id: validate
        env:
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
        run: |
          set -uo pipefail
          LOG="$RUNNER_TEMP/altool-validate.log"
          if xcrun altool --validate-app -f "$IPA_PATH" -t ios \
            --apiKey "$APPLE_API_KEY" --apiIssuer "$APPLE_API_ISSUER" 2>&1 | tee "$LOG"; then
            echo "validated=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          CODES=$(grep -oE '\(9[0-9]{4}\)' "$LOG" | tr -d '()' | sort -u)
          echo "App Store Connect error codes: ${CODES:-none}"
          if [ -n "$CODES" ] && [ -z "$(echo "$CODES" | grep -vE '^(90186|90062)$')" ]; then
            echo "validated=false" >> "$GITHUB_OUTPUT"
            echo "::warning title=App Store upload skipped::Version $IOS_VERSION is already approved or its pre-release train is closed, so App Store Connect will not accept more builds for it. The signed IPA is still attached as an artifact. Ship the next App Store build by cutting a release whose derived version is higher."
            exit 0
          fi
          exit 1

      - name: Render App Store release notes
        if: steps.validate.outputs.validated == 'true'
        run: |
          set -euo pipefail
          NOTES_VERSION=$(node -p "require('./packages/client/package.json').version")
          node --experimental-strip-types scripts/render-release-notes.ts \
            --version="$NOTES_VERSION" --platform=ios --max=4000 \
            --out="$RUNNER_TEMP/whats-new.txt"
          cat "$RUNNER_TEMP/whats-new.txt"

      - name: Set up Python
        if: steps.validate.outputs.validated == 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install App Store Connect CLI
        if: steps.validate.outputs.validated == 'true'
        run: pip install --disable-pip-version-check codemagic-cli-tools==0.69.0

      - name: Upload and submit to App Store
        if: steps.validate.outputs.validated == 'true'
        env:
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APPLE_API_ISSUER }}
          APP_STORE_CONNECT_KEY_IDENTIFIER: ${{ secrets.APPLE_API_KEY }}
        run: |
          set -euo pipefail
          app-store-connect publish \
            --path "$IPA_PATH" \
            --app-store \
            --platform IOS \
            --version-string "$IOS_VERSION" \
            --release-type AFTER_APPROVAL \
            --cancel-previous-submissions \
            --whats-new "@file:$RUNNER_TEMP/whats-new.txt" \
            --max-build-processing-wait 45

      - name: Upload IPA artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ios-appstore-ipa
          path: packages/wrapper/src-tauri/gen/apple/build/*/*.ipa
          if-no-files-found: warn

      - name: Upload iOS debug symbols to Sentry
        if: always()
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: colibri-social
          SENTRY_PROJECT: rust-native-app
        run: |
          DSYMS=packages/wrapper/src-tauri/gen/apple/build/colibri-social_iOS.xcarchive/dSYMs
          if [ -z "${SENTRY_AUTH_TOKEN:-}" ] || [ ! -d "$DSYMS" ]; then
            echo "No Sentry auth token or no dSYMs to upload, skipping."
            exit 0
          fi
          pnpm dlx @sentry/cli debug-files upload "$DSYMS"

  homebrew:
    needs: publish-tauri
    runs-on: macos-latest
    steps:
      - name: Mint tap token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          owner: colibri-social
          repositories: homebrew-tap

      - name: Update Homebrew cask
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TOKEN: ${{ steps.app-token.outputs.token }}
          VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          TAG="v$VERSION"
          REPO="${{ github.repository }}"
          ASSET=$(gh release view "$TAG" --repo "$REPO" --json assets --jq '[.assets[].name | select(test("\\.dmg$"))][0] // ""')
          test -n "$ASSET"
          gh release download "$TAG" --repo "$REPO" --pattern "$ASSET" --dir dl
          SHA=$(shasum -a 256 "dl/$ASSET" | cut -d' ' -f1)
          ENC=$(jq -rn --arg s "$ASSET" '$s|@uri')
          URL="https://github.com/$REPO/releases/download/$TAG/$ENC"
          TAP_DIR="$(brew --repository)/Library/Taps/colibri-social/homebrew-tap"
          rm -rf "$TAP_DIR"
          mkdir -p "$(dirname "$TAP_DIR")"
          git clone --depth 1 "https://x-access-token:$TOKEN@github.com/colibri-social/homebrew-tap.git" "$TAP_DIR"
          echo "TAP_DIR=$TAP_DIR" >> "$GITHUB_ENV"
          mkdir -p "$TAP_DIR/Casks"
          cat > "$TAP_DIR/Casks/colibri-social.rb" <<RUBY
          cask "colibri-social" do
            version "$VERSION"
            sha256 "$SHA"

            url "$URL"
            name "Colibri Social"
            desc "Next-generation social chat app built on the AT Protocol"
            homepage "https://colibri.social"

            app "Colibri Social.app"

            zap trash: [
              "~/Library/Application Support/social.colibri.app",
              "~/Library/Caches/social.colibri.app",
              "~/Library/Preferences/social.colibri.app.plist",
              "~/Library/Saved Application State/social.colibri.app.savedState",
            ]
          end
          RUBY

      # Installs the generated cask exactly as a user would, then checks the
      # bundle seal, notarization ticket and Gatekeeper verdict. Anything the
      # cask does to the app bundle that invalidates its signature fails here,
      # before the tap ever serves it.
      - name: Verify cask install
        env:
          HOMEBREW_NO_AUTO_UPDATE: "1"
        run: |
          set -euo pipefail
          brew install --cask colibri-social/tap/colibri-social
          APP="/Applications/Colibri Social.app"
          test -d "$APP"
          codesign --verify --deep --strict --verbose=2 "$APP"
          xcrun stapler validate "$APP"
          spctl --assess --type exec --verbose=4 "$APP"

      - name: Push cask
        env:
          VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          cd "$TAP_DIR"
          git config user.name "colibri-release[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Casks/colibri-social.rb
          git diff --cached --quiet && exit 0
          git commit -m "colibri-social $VERSION"
          git branch -M main
          git push origin HEAD:main

  scoop:
    needs: publish-tauri
    runs-on: ubuntu-latest
    steps:
      - name: Mint bucket token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          owner: colibri-social
          repositories: scoop-bucket

      - name: Update Scoop manifest
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TOKEN: ${{ steps.app-token.outputs.token }}
          VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          TAG="v$VERSION"
          REPO="${{ github.repository }}"
          ASSET=$(gh release view "$TAG" --repo "$REPO" --json assets --jq '[.assets[].name | select(test("setup\\.exe$"))][0] // ""')
          test -n "$ASSET"
          gh release download "$TAG" --repo "$REPO" --pattern "$ASSET" --dir dl
          SHA=$(sha256sum "dl/$ASSET" | cut -d' ' -f1)
          ENC=$(jq -rn --arg s "$ASSET" '$s|@uri')
          URL="https://github.com/$REPO/releases/download/$TAG/$ENC#/dl.7z"
          git clone --depth 1 "https://x-access-token:$TOKEN@github.com/colibri-social/scoop-bucket.git" bucket
          mkdir -p bucket/bucket
          jq -n --arg v "$VERSION" --arg url "$URL" --arg hash "$SHA" '{
            version: $v,
            description: "Next-generation social chat app built on the AT Protocol",
            homepage: "https://colibri.social",
            license: "MIT",
            architecture: { "64bit": { url: $url, hash: $hash } },
            post_install: ["Remove-Item \"$dir\\`$PLUGINSDIR\" -Recurse -Force -ErrorAction SilentlyContinue"],
            shortcuts: [["Colibri Social.exe", "Colibri Social"]]
          }' > bucket/bucket/colibri-social.json
          cd bucket
          git config user.name "colibri-release[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add bucket/colibri-social.json
          git diff --cached --quiet && exit 0
          git commit -m "colibri-social $VERSION"
          git branch -M main
          git push origin HEAD:main

  play:
    needs: publish-tauri
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: setup node
        uses: actions/setup-node@v6
        with:
          node-version: lts/*

      - name: Download AAB artifact
        uses: actions/download-artifact@v4
        with:
          name: android-aab
          path: dl

      - name: Render Play release notes
        run: |
          set -euo pipefail
          mkdir -p whatsnew
          NOTES_VERSION=$(node -p "require('./packages/client/package.json').version")
          node --experimental-strip-types scripts/render-release-notes.ts \
            --version="$NOTES_VERSION" --platform=android --max=500 \
            ${{ inputs.play_track == 'production' && '--strict' || '' }} \
            --out=whatsnew/whatsnew-en-US
          cat whatsnew/whatsnew-en-US

      - name: Compute Play release name (production only)
        if: inputs.play_track == 'production'
        env:
          VERSION: ${{ inputs.version }}
        run: echo "PLAY_RELEASE_NAME=$VERSION ($(date -u +%F))" >> "$GITHUB_ENV"

      - name: Upload to Play (testing track)
        if: inputs.play_track != 'production'
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: social.colibri.app
          releaseFiles: dl/*.aab
          track: ${{ inputs.play_track }}
          status: completed
          whatsNewDirectory: whatsnew

      - name: Upload to Play (production, staged rollout)
        if: inputs.play_track == 'production'
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: social.colibri.app
          releaseFiles: dl/*.aab
          track: production
          status: inProgress
          userFraction: 0.2
          releaseName: ${{ env.PLAY_RELEASE_NAME }}
          whatsNewDirectory: whatsnew

      - name: Mirror onto the testing tracks
        if: inputs.play_mirror_testing
        env:
          PLAY_SERVICE_ACCOUNT_JSON: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          SOURCE_TRACK: ${{ inputs.play_track }}
        run: |
          set -euo pipefail
          TARGETS=""
          for TRACK in alpha beta; do
            if [ "$TRACK" != "$SOURCE_TRACK" ]; then
              TARGETS="${TARGETS:+$TARGETS,}$TRACK"
            fi
          done
          test -n "$TARGETS"
          node --experimental-strip-types scripts/play-mirror.ts \
            --from="$SOURCE_TRACK" --to="$TARGETS" \
            --version-code="$((1000 + GITHUB_RUN_NUMBER))" \
            --notes=whatsnew/whatsnew-en-US

  msstore-build:
    if: ${{ !inputs.appstore_only }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - name: setup pnpm
        uses: pnpm/action-setup@v6

      - name: setup node
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: "pnpm"

      - name: install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache twemoji assets
        uses: actions/cache@v4
        with:
          path: packages/assets/files/twemoji
          key: twemoji-${{ runner.os }}-${{ hashFiles('packages/assets/scripts/fetch-emoji-assets.mjs', 'packages/client/package.json') }}
          restore-keys: twemoji-${{ runner.os }}-

      - name: Fetch twemoji assets
        run: pnpm --filter @colibri-social/assets fetch-emoji

      - name: Build frontend dependencies
        run: pnpm --filter "@colibri-social/wrapper^..." build

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "packages/wrapper/src-tauri -> target"
          shared-key: tauri-windows-latest
          cache-on-failure: true
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: install msixbundle-cli
        shell: bash
        run: cargo install msixbundle-cli --locked

      - name: verify msixbundle-cli runs
        shell: pwsh
        run: msixbundle-cli --version

      - name: Sync version
        run: node packages/wrapper/scripts/set-version.mjs "${{ inputs.version }}"

      - name: Compute Store version and Windows build overrides
        shell: bash
        run: |
          set -euo pipefail
          APP_MAJOR=$(echo "${{ inputs.version }}" | cut -d. -f1)
          APP_MINOR=$(echo "${{ inputs.version }}" | cut -d. -f2)
          STORE_MINOR=$((10#$APP_MAJOR * 1000 + 10#$APP_MINOR))
          STORE_VERSION="1.${STORE_MINOR}.${GITHUB_RUN_NUMBER}"
          echo "MSIX Store version (padded to .0 by the bundler): ${STORE_VERSION}.0"
          cat > packages/wrapper/src-tauri/tauri.windows.conf.json <<JSON
          {
            "identifier": "LouisEscher.ColibriSocial",
            "version": "${STORE_VERSION}",
            "mainBinaryName": "ColibriSocial"
          }
          JSON

      - name: Build MSIX bundle (x64)
        working-directory: packages/wrapper
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_UPDATER_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_UPDATER_PASSWORD }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          VITE_SENTRY_DSN: ${{ secrets.WRAPPER_SENTRY_DSN }}
          SENTRY_DSN: ${{ secrets.NATIVE_SENTRY_DSN }}
        run: pnpm dlx --config.optional=false @choochmeque/tauri-windows-bundle@0.1.28 build --arch x64 --runner pnpm --verbose

      - name: Upload MSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: msix-store
          path: packages/wrapper/src-tauri/target/msix/*.msixbundle
          if-no-files-found: error

  msstore-deploy:
    needs: msstore-build
    runs-on: windows-latest
    steps:
      - name: Download MSIX artifact
        uses: actions/download-artifact@v4
        with:
          name: msix-store
          path: dl

      - name: Configure Microsoft Store CLI
        uses: microsoft/microsoft-store-apppublisher@v1.1

      - name: Reconfigure store credentials
        shell: pwsh
        env:
          STORE_TENANT_ID: ${{ secrets.AZURE_AD_TENANT_ID }}
          STORE_SELLER_ID: ${{ secrets.SELLER_ID }}
          STORE_CLIENT_ID: ${{ secrets.AZURE_AD_APPLICATION_CLIENT_ID }}
          STORE_CLIENT_SECRET: ${{ secrets.AZURE_AD_APPLICATION_SECRET }}
        run: |
          $ErrorActionPreference = "Stop"
          $tenantId = "$env:STORE_TENANT_ID".Trim()
          $sellerId = "$env:STORE_SELLER_ID".Trim()
          $clientId = "$env:STORE_CLIENT_ID".Trim()
          $clientSecret = "$env:STORE_CLIENT_SECRET".Trim()
          foreach ($pair in @(
            @("AZURE_AD_TENANT_ID", $tenantId),
            @("SELLER_ID", $sellerId),
            @("AZURE_AD_APPLICATION_CLIENT_ID", $clientId),
            @("AZURE_AD_APPLICATION_SECRET", $clientSecret)
          )) {
            if ([string]::IsNullOrWhiteSpace($pair[1])) { throw "Repository secret $($pair[0]) is empty or unset." }
          }
          if ($sellerId -notmatch '^\d+$') {
            throw "Repository secret SELLER_ID must be the numeric Seller Id from Partner Center (Account settings > Organization profile > Legal info). The stored value is $($sellerId.Length) characters and is not all digits."
          }
          if ($tenantId -notmatch '^[0-9a-fA-F]{8}-([0-9a-fA-F]{4}-){3}[0-9a-fA-F]{12}$') {
            throw "Repository secret AZURE_AD_TENANT_ID must be a GUID."
          }
          if ($clientId -notmatch '^[0-9a-fA-F]{8}-([0-9a-fA-F]{4}-){3}[0-9a-fA-F]{12}$') {
            throw "Repository secret AZURE_AD_APPLICATION_CLIENT_ID must be a GUID."
          }
          msstore reconfigure --tenantId $tenantId --sellerId $sellerId --clientId $clientId --clientSecret $clientSecret
          if ($LASTEXITCODE -ne 0) { throw "msstore reconfigure exited with code $LASTEXITCODE" }

      - name: Publish MSIX to Microsoft Store
        shell: pwsh
        run: |
          $bundle = Get-ChildItem -Path dl -Filter *.msixbundle | Select-Object -First 1
          if (-not $bundle) { throw "No .msixbundle found in dl/" }
          Write-Host "Publishing $($bundle.Name) to product 9P8Q77FCCSWR"
          msstore publish $bundle.FullName -id 9P8Q77FCCSWR --verbose
