---
title: AppView
description: What the Colibri AppView is, what it runs on, and what it is responsible for.
sidebar:
  order: 3
---

import AppViewDiagram from "../../../../components/docs/AppViewDiagram.astro";

Colibri's AppView is what you would call the backend in a conventional
full-stack application. It serves the XRPC API and the event socket, holds each
community's credentials and writes records on its behalf, keeps a synced copy of
every space it serves, and hosts the voice SFU.

:::note
This page describes the [reference implementation](https://github.com/colibri-social/api).
For the interface it implements, see the
[specification](/docs/specification/appview).
:::

## What the AppView is

A typical atproto AppView subscribes to a relay, consumes a firehose of public
records, and indexes what it cares about. Colibri cannot work that way. Its data
lives in [permissioned spaces](/docs/architecture/spaces), and there is no relay for
permissioned data.

The AppView therefore pulls each member's repository from that member's own
host, holding a credential the community issued it. There is no firehose subscription
for community content and no public repository indexing.
[Sync](/docs/architecture/sync) covers how that works.

<AppViewDiagram />

## Reachability is a hard requirement

Every community space uses a `managing-app` policy naming the AppView, so the
community's PDS calls
`com.atproto.simplespace.checkUserAccess` before it mints any credential.

An AppView that cannot be reached from wherever the community's PDS runs makes
its communities unreadable, even with the PDS itself perfectly healthy. It also
costs writes made during the outage their place in the
[writer set](/docs/architecture/sync#the-writer-set).

## Authentication

Clients authenticate with an atproto service auth JWT that their own PDS minted
for one specific method, sent in the `Authorization` header. The usual way to
get one is to call the AppView *through* the user's PDS with an `atproto-proxy`
header, which makes the PDS mint and attach the token.

On top of the standard checks, the AppView enforces a **maximum token
lifetime**. An issuer picks its own expiry, so a token captured from a log or a
proxy stays replayable for however far ahead that was set.

## Voice

Voice and video run on a [mediasoup](https://mediasoup.org) SFU embedded in the
AppView process rather than a separate service. Session setup and control happen
over a dedicated signalling WebSocket, not over XRPC. Only moderation actions
have an XRPC method.

## See also

- [Permissioned Spaces](/docs/architecture/spaces): the primitive everything here rests on.
- [Communities](/docs/architecture/communities): what a community is made of.
- [Sync](/docs/architecture/sync): keeping a copy of a space current without a relay.
- [Storage](/docs/architecture/storage): what lands in the database and what can be rebuilt.
- [Specification](/docs/specification/appview): the endpoint and event reference.
- [Self-hosting](/docs/self-hosting/getting-started): running one.
