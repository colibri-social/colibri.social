---
title: AppView
---

import XRPCEndpointCard from "../../../../components/docs/XRPCEndpointCard.astro";

The Colibri AppView is responsible for aggregating message data from across the network and providing endpoints for interacting with this data. Any requests to the AppView are meant to be proxied via the user's PDS, to which the client application makes the requests. When requests are forwarded, the following DID URI should be used to indicate the target:

```
did:web:api.colibri.social#colibri_appview
```

The push-registration endpoints [`registerPush`](#socialcolibrinotificationregisterpush) and [`unregisterPush`](#socialcolibrinotificationunregisterpush) instead target the notification service:

```
did:web:api.colibri.social#colibri_notif
```

Both services resolve to the same endpoint today, so this only affects which `atproto-proxy` fragment clients send; using `#colibri_notif` for push registration keeps the declared service boundary correct if the notification service is ever split onto its own deployment. The notification *feed* reads (`listNotifications`, `getUnreadCount`, `updateSeen`, `updateSeenForMessage`, `getUnseen`) remain on `#colibri_appview`.

## Implemented Endpoints

The AppView supports a range of XRPC methods that PDSs can call. All `com.atproto` endpoints adhere to the original specification. There are also certain `social.colibri` XRPC endpoints which allow for extended functionality. All of these endpoints are available under the `/xrpc/` path.

:::danger[Authentication]
If an endpoint requires authentication, the request must carry a [service auth](https://atproto.com/specs/xrpc#inter-service-authentication-jwt) token (a JWT signed by the caller's repo signing key, with `aud` set to `did:web:api.colibri.social` and `lxm` set to the method NSID).

The recommended way to supply it is to call the AppView **through the user's PDS** using standard service proxying: send the request to the PDS with the `atproto-proxy: did:web:api.colibri.social#colibri_appview` header, and the PDS mints the service-auth token and attaches it as the `Authorization: Bearer <jwt>` header on the proxied request.

The [`social.colibri.sync.subscribeEvents`](#socialcolibrisyncsubscribeevents) WebSocket is the exception: browsers cannot set request headers (or use PDS service proxying) on a `WebSocket`, so the token is supplied as a WebSocket subprotocol instead. See that endpoint for details.
:::

### `com.atproto.identity.resolveDid`

Resolves a DID to a DID document. Does not bi-directionally verify the handle.

<XRPCEndpointCard
	method="GET"
	nsid="com.atproto.identity.resolveDid"
	queryParams={{
		did: {
			type: "did",
			required: true
		}
	}}
	response={{
		didDoc: {
			type: "DID Document",
			required: true
		}
	}}
/>

### `com.atproto.identity.resolveHandle`

Resolves an atproto handle (hostname) to a DID. Does not necessarily bi-directionally verify against the DID document.

<XRPCEndpointCard
	method="GET"
	nsid="com.atproto.identity.resolveHandle"
	queryParams={{
		handle: {
			type: "handle",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true
		}
	}}
/>

### `com.atproto.identity.resolveIdentity`

Resolves an atproto handle or DID to a DID document. Handles are resolved to a DID first, then the DID document is fetched.

<XRPCEndpointCard
	method="GET"
	nsid="com.atproto.identity.resolveIdentity"
	queryParams={{
		identity: {
			type: "did | handle",
			required: true
		}
	}}
	response={{
		didDoc: {
			type: "DID Document",
			required: true
		}
	}}
/>

### `com.atproto.sync.getBlob`

Proxies a blob fetch to the PDS that hosts the given DID. Blobs are not stored locally.

<XRPCEndpointCard
	method="GET"
	nsid="com.atproto.sync.getBlob"
	queryParams={{
		did: {
			type: "did",
			required: true
		},
		cid: {
			type: "cid",
			required: true
		}
	}}
	response={{
		blob: {
			type: "bytes",
			required: true
		}
	}}
/>

### `com.atproto.sync.getRecord`

Get a single cached record from the AppView.

<XRPCEndpointCard
	method="GET"
	nsid="com.atproto.sync.getRecord"
	queryParams={{
		repo: {
			type: "at-identifier",
			required: true
		},
		collection: {
			type: "nsid",
			required: true
		},
		rkey: {
			type: "record-key",
			required: true
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		},
		value: {
			type: "object",
			required: true
		}
	}}
/>

### `com.atproto.sync.listRecords`

List a range of cached records in a repository, matching a specific collection.

<XRPCEndpointCard
	method="GET"
	nsid="com.atproto.sync.listRecords"
	queryParams={{
		repo: {
			type: "at-identifier",
			required: true
		},
		collection: {
			type: "nsid",
			required: true
		},
		limit: {
			type: "integer",
			required: false,
		},
		cursor: {
			type: "string",
			required: false,
		},
		reverse: {
			type: "boolean",
			required: false,
		}
	}}
	response={{
		cursor: {
			type: "string",
			required: false,
		},
		records: {
			type: "object[]",
			required: true
		}
	}}
/>

---

### `social.colibri.server.describeServer`

Identifies the server as a Colibri AppView and reports its flavor and version.
	method="GET"
	nsid="social.colibri.server.describeServer"
	response={{
		software: {
			type: "string",
			required: true
		},
		flavor: {
			type: "string",
			required: true
		},
		version: {
			type: "string",
			required: true
		}
	}}
/>

`software` is always `"colibri-appview"`. This is the field clients key on to decide whether a host is a Colibri AppView. `flavor` identifies the AppView build (`"vanilla"` for the stock AppView; forks may set any arbitrary string). `version` is the running crate version.

### `social.colibri.actor.getData`

Get the status data and profile for a Colibri user.

Profile fields (`displayName`, `avatar`, `banner`, `description`) are sourced from the user's `social.colibri.actor.profile` record. When that record has `syncBluesky: true`, the AppView instead serves these four fields live from the user's `app.bsky.actor.profile` record (Bluesky stays the source of truth). If the user has no `social.colibri.actor.profile` record yet (not onboarded), the AppView falls back to `app.bsky.actor.profile`. The Colibri-only `theme` is always taken from the Colibri profile record, and `isBot` is always derived from the Bluesky self-label convention.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.actor.getData"
	queryParams={{
		identifier: {
			type: "did | handle",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true,
		},
		handle: {
			type: "handle",
			required: true,
		},
		data: {
			type: "object",
			required: true,
			children: {
				displayName: {
					type: "string",
					required: true,
				},
				avatar: {
					type: "blob",
					required: false,
				},
				banner: {
					type: "blob",
					required: false,
				},
				description: {
					type: "string",
					required: false,
				},
				isBot: {
					type: "boolean",
					required: true,
				},
				onlineState: {
					type: "string",
					required: true,
				},
				syncBluesky: {
					type: "boolean",
					required: true,
				},
				theme: {
					type: "object",
					required: false,
					children: {
						accentColor: {
							type: "string",
							required: false,
						},
						gradient: {
							type: "object",
							required: false,
							children: {
								primary: {
									type: "string",
									required: false,
								},
								secondary: {
									type: "string",
									required: false,
								}
							}
						},
						bannerColor: {
							type: "string",
							required: false,
						}
					}
				},
				status: {
					type: "object",
					required: true,
					children: {
						emoji: {
							type: "string",
							required: false,
						},
						text: {
							type: "string",
							required: true
						}
					}
				},
			}
		}
	}}
/>

### `social.colibri.actor.setState`

Sets the online state for a Colibri user.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.actor.setState"
	authenticated
	queryParams={{
		state: {
			type: "string",
			required: true
		}
	}}
	response={{
		onlineState: {
			type: "string",
			required: true
		}
	}}
/>

### `social.colibri.actor.listCommunities`

Lists all communities the Colibri user is a part of (and not banned from).

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.actor.listCommunities"
	authenticated
	response={{
		communities: {
			type: "object[]",
			required: true,
			children: {
				name: {
					type: "string",
					required: true
				},
				picture: {
					type: "blob",
					required: false
				},
				description: {
					type: "string",
					required: false
				},
				categoryOrder: {
					type: "string[]",
					required: false
				},
				uri: {
					type: "at-uri",
					required: true
				},
				requiresApprovalToJoin: {
					type: "boolean",
					required: true
				},
				isLegacy: {
					type: "boolean",
					required: false
				},
				isOwner: {
					type: "boolean",
					required: false
				},
			}
		}
	}}
/>

`isOwner` reports whether the authenticated caller owns the community (user holding the "Owner" role).

### `social.colibri.actor.listMutes`

Lists every channel or community the authenticated user has muted. Each entry is one `social.colibri.actor.mute` record, `subject` is the AT-URI of the muted channel or community.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.actor.listMutes"
	authenticated
	response={{
		mutes: {
			type: "object[]",
			required: true,
			children: {
				uri: {
					type: "at-uri",
					required: true,
				},
				subject: {
					type: "at-uri",
					required: true,
				}
			}
		}
	}}
/>

### `social.colibri.sync.subscribeEvents`

Opens a connection with a WebSocket stream that transmits relevant events for the user.

<XRPCEndpointCard
	method="WS"
	nsid="social.colibri.sync.subscribeEvents"
	authenticated
/>

#### Authentication

Unlike the HTTP endpoints, this WebSocket cannot be PDS-proxied: browsers expose no way to set an `Authorization` header (or any custom header) on a `WebSocket`. The token is therefore passed through the one handshake field a browser *can* control: the subprotocol list (`Sec-WebSocket-Protocol`).

The client mints a short-lived [service auth](https://atproto.com/specs/xrpc#inter-service-authentication-jwt) token (`aud: did:web:api.colibri.social`, `lxm: social.colibri.sync.subscribeEvents`) and opens the socket offering two subprotocols, in order:

1. `colibri.auth.bearer`: a fixed sentinel marking that the next entry is the token.
2. the service-auth JWT itself.

```js
new WebSocket(url, ["colibri.auth.bearer", token]);
```

A service-auth JWT is base64url (`[A-Za-z0-9._-]`), every character of which is a valid subprotocol token, so it passes through the handshake unmodified. The AppView reads the entry following the `colibri.auth.bearer` sentinel, verifies it, and **echoes the `colibri.auth.bearer` sentinel back** in the response `Sec-WebSocket-Protocol` header, per RFC 6455 a browser that offered a subprotocol fails the connection unless the server selects one, so this echo is required. The JWT itself is never echoed back.

The `auth` query parameter is still accepted as a transitional / local-dev fallback; when both are present, the subprotocol token wins.

#### Events

The event stream can choose to transmit events to clients who have signed up to receive them. All events follow the same JSON data structure: they have a `type` (which is the same as the heading below), and an optional `data` field.

Events are derived from the underlying records, which reference sibling records by bare record key. Every record reference in an event payload — `categoryOrder`, `channelOrder`, `category`, `community`, `roles`, `allowedRoles` — is nonetheless emitted as a **full AT URI**, matching what the read endpoints return, so clients can compare the two without translating.

##### `ack`

Sent when the AppView receives a [`heartbeat`](#heartbeat) message from the client. This event carries no data.

##### `community_event`

Events of this type are sent to clients when a community has been updated or deleted. If the `event` is set to `delete`, only the community's `uri` needs to be supplied.

```ts title="data"
{
	event: 'upsert' | 'delete';
	uri: string;
	name?: string;
	description?: string;
	picture?: blob;
	categoryOrder?: string[];
	requiresApprovalToJoin?: boolean;
}
```

##### `member_event`

Events of this type are sent to clients when a member joins a community, has their roles changed, or leaves. `member_event` is broadcast to all connected clients (not just the subject). Clients should check `community` to scope the update to the right community, and check `member.did` against their own DID to detect when they themselves have been admitted or had their roles updated.

| `event` | When | `membership` | `member` | `memberDid` |
|---|---|---|---|---|
| `join` | A new member record is written (auto-admit or `approveMembership`) | AT-URI of the user's membership declaration, if available | Always present | Absent |
| `roles_updated` | An existing member record is updated (e.g. a moderator changes roles) | Absent | Always present with the new `roles` array | Absent |
| `leave` | A member record is deleted (kick, ban, or self-leave). Sent to all remaining members | Absent | Absent | Always present |

```ts title="data"
{
	event: 'join' | 'roles_updated' | 'leave';
	community: string;
	membership?: string;
	member?: {
		did: string;
		handle: string;
		roles: string[];
		joinedAt: string;
		nickname?: string;
		data: {
			displayName: string;
			avatar?: blob;
			banner?: blob;
			description?: string;
			isBot: boolean;
			onlineState: string;
			status: {
				text: string;
				emoji?: string;
			};
		};
	};
	/** DID of the member who left, only present on `leave` events. */
	memberDid?: string;
}
```

##### `application_event`

Events of this type are sent to clients for changes to the moderator-facing pending-applications queue of a `requiresApprovalToJoin` community. Broadcast to all connected clients.

| `event` | When | `did` / `handle` / `createdAt` / `data` |
|---|---|---|
| `create` | A new `social.colibri.membership` is indexed for a closed community, or a kicked member's original membership record is still on file and the community currently requires approval | Always present |
| `resolve` | The application was admitted via `approveMembership` | Absent |
| `dismiss` | A moderator hid the application from the active queue via `dismissApplication` (off-protocol) | Absent |
| `undismiss` | A dismissed application was restored via `undismissApplication` (off-protocol) | Absent |

```ts title="data"
{
	event: 'create' | 'resolve' | 'dismiss' | 'undismiss';
	community: string;
	membership: string;
	did?: string;
	handle?: string;
	createdAt?: string;
	data?: {
		displayName: string;
		avatar?: blob;
		banner?: blob;
		description?: string;
		isBot: boolean;
		onlineState: string;
		status: {
			text: string;
			emoji?: string;
		};
	};
}
```

##### `category_event`

Events of this type are sent to clients when a category has been created, updated, or deleted. When the `event` is `delete`, all data except the `uri` and `event` can be omitted.

```ts title="data"
{
	event: 'upsert' | 'delete';
	uri: string;
	community?: string;
	name?: string;
	channelOrder?: string[];
}
```

##### `channel_event`

Events of this type are sent to clients when a channel has been created, updated, or deleted. When the `event` is `delete`, all data except the `uri` and `event` can be omitted. On `upsert`, `category` is always present so clients can place the channel in the correct category without a follow-up fetch.

```ts title="data"
{
	event: 'upsert' | 'delete';
	uri: string;
	community?: string;
	category?: string;
	name?: string;
	description?: string;
	type?: string;
	ownerOnly?: boolean;
	allowedRoles?: string[];
	allowedMembers?: string[];
}
```

##### `role_event`

Events of this type are sent to clients when a role has been created, updated, or deleted. When the `event` is `delete`, all data except `uri` is omitted. On `upsert`, `community`, `name`, `permissions`, and `position` are always present.

```ts title="data"
{
	event: 'upsert' | 'delete';
	uri: string;
	community?: string;
	name?: string;
	color?: string;
	permissions?: string[];
	position?: number;
	hoisted?: boolean;
	mentionable?: boolean;
}
```

##### `message_event`

Events of this type are sent to clients when a message has been sent, edited, or deleted. When the `event` is `delete`, all data except the `uri` and `event` can be omitted. On `upsert`, the `author` object is always present.

```ts title="data"
{
	event: 'upsert' | 'delete';
	uri: string;
	channel?: string;
	text?: string;
	facets?: facet[];
	createdAt?: string;
	edited?: boolean;
	parent?: string;
	attachments?: { blob: blob; name?: string; }[];
	author?: {
		did: string;
		handle: string;
		data: {
			displayName: string;
			avatar?: blob;
			banner?: blob;
			description?: string;
			isBot: boolean;
			onlineState: string;
			status: {
				text: string;
				emoji?: string;
			};
		};
	};
}
```

##### `reaction_event`

Events of this type are sent to clients when a reaction has been added to or removed from a message.

```ts title="data"
{
	event: 'added' | 'removed';
	uri: string;
	emoji?: string;
	target?: string;
}
```

##### `user_event`

Events of this type are sent to clients when a user the client knows about has updated their Bluesky profile or Colibri status.

```ts title="data"
{
	did: string;
	status?: {
		emoji?: string;
		text: string;
		state: 'online' | 'away' | 'dnd' | 'offline';
	};
	profile: {
		displayName?: string;
		avatar?: blob;
		banner?: blob;
		description?: string;
		isBot: boolean;
		handle: string;
	};
}
```

##### `typing_event`

Events of this type are sent to clients when a user is typing in the channel the user is currently viewing. For notes on how to report this to the AppView, see the [`view`](#view) message type.

```ts title="data"
{
	event: 'start' | 'stop';
	channel: string;
	did: string;
}
```

#### Messages

The following messages can be sent from the client to the AppView. All messages follow the same JSON data structure: they have a `type` (which is the same as the heading below), and an optional `data` field.

##### `heartbeat`

A generic event that carries no data. It is used to keep the socket connection alive. The AppView will always respond with an `ack` event.

##### `typing`

Clients send a `typing` message to report that the current user is typing in a channel. The AppView broadcasts a [`typing_event`](#typing_event) to every other client that is currently viewing the same channel (determined by the last `view` message they sent). No response is sent back to the sender.

```ts title="data"
{
	channel: string;
}
```

##### `view`

Clients can send a `view` message to the AppView to inform it about the channel the user is currently viewing. The AppView uses this to determine who should receive [`typing_event`](#typing_event) broadcasts.

```ts title="data"
{
	channel: string;
}
```

##### `voice_join`

Clients must send a `voice_join` message to the AppView when the user joins a voice channel.

```ts title="data"
{
	channel: string;
	community: string;
}
```

##### `voice_leave`

Clients must send a `voice_leave` message to the AppView when the user leaves a voice channel.

### `social.colibri.community.create`

Mints a new community DID on the AppView's own PDS and bootstraps a fully populated community. The AppView authenticates against `PDS_LOC` using admin credentials so it can create accounts without an invite code, stores the new account's credentials encrypted in `community_credentials`, and writes five records to the new repo:

- `social.colibri.community` (pinned at `rkey: "self"`)
- `social.colibri.category`: a default `"General"` category
- `social.colibri.channel`: a default `"general"` text channel inside that category
- `social.colibri.role`: an `"Owner"` role with every permission and `protected: true` so role-management endpoints refuse to delete it
- `social.colibri.member`: `subject` = authenticated caller, holding the Owner role

The caller ends up as an owner-equivalent member of the new community. Community picture and banner are uploaded by sending their contents as a multipart body. If supplied, the picture is validated against the allowed MIME types (`image/jpeg`, `image/png`, `image/gif`), uploaded to the community's PDS, and stored as the community picture. An empty body means no picture.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.create"
	authenticated
	queryParams={{
		name: {
			type: "string",
			required: true
		},
		description: {
			type: "string",
			required: false
		},
		requiresApprovalToJoin: {
			type: "boolean",
			required: false
		},
	}}
	body={{
		image: {
			type: "bytes",
			required: false
		}
	}}
	response={{
		did: {
			type: "did",
			required: true
		},
		community: {
			type: "at-uri",
			required: true
		},
		category: {
			type: "at-uri",
			required: true
		},
		channel: {
			type: "at-uri",
			required: true
		},
		ownerRole: {
			type: "at-uri",
			required: true
		},
		member: {
			type: "at-uri",
			required: true
		}
	}}
/>

The AppView identifies communities by DID; handles minted alongside the account are an implementation detail of PDS hosting and are not expected to be used for identity resolution. The AppView fails to start if any of `PDS_LOC`, `APPVIEW_HANDLE_DOMAIN`, or `CREDENTIAL_ENCRYPTION_KEY` are missing.

### `social.colibri.community.update`

Updates the community's settings. Only fields that are supplied are changed. New community picture and banner are uploaded by sending their contents as a multipart body.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.update"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		name: {
			type: "string",
			required: false
		},
		description: {
			type: "string",
			required: false
		},
		requiresApprovalToJoin: {
			type: "boolean",
			required: false
		}
	}}
	body={{
		image: {
			type: "bytes",
			required: false
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `community_event { event: "upsert", uri, name?, description?, picture?, requiresApprovalToJoin? }`.

### `social.colibri.community.delete`

Deletes a community. Requires the caller to hold the `community.delete` permission. For AppView-managed communities (those minted via [`create`](#socialcolibricommunitycreate)) the entire PDS account is torn down via `com.atproto.admin.deleteAccount`, removing the repo and every record on it. For BYO communities the AppView does not administer the hosting PDS, so the account is left intact; the AppView only stops tracking it.

In both cases the stored credentials and every locally cached record for the community DID are removed, so the community no longer surfaces through any read endpoint.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.delete"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true
		}
	}}
/>

### `social.colibri.community.registerCredentials`

Registers user-supplied credentials (PDS endpoint + identifier + app password) for an existing community DID hosted on a PDS the AppView doesn't manage. The AppView verifies proof-of-control by performing a `com.atproto.server.createSession` against the supplied PDS.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.registerCredentials"
	authenticated
	queryParams={{
		did: {
			type: "did",
			required: true
		},
		pds: {
			type: "uri",
			required: true
		},
		identifier: {
			type: "at-identifier",
			required: true
		},
		password: {
			type: "string",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true
		},
		source: {
			type: "string",
			required: true
		}
	}}
/>

### `social.colibri.community.approveMembership`

Admits a pending applicant to a closed community (`requiresApprovalToJoin: true`) by writing the community-side `social.colibri.member` record. Requires `approval.manage` permission.

On admission, broadcasts `application_event` and clears any dismissal row for the subject.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.approveMembership"
	authenticated
	queryParams={{
		membership: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true
		},
		community: {
			type: "at-uri",
			required: true
		},
		member: {
			type: "at-uri",
			required: false
		}
	}}
/>

### `social.colibri.community.listApplications`

Lists the pending join applications for a community that requires approval (`requiresApprovalToJoin: true`). A pending application is an indexed `social.colibri.membership` targeting the community whose author does not yet hold a `social.colibri.member` record on the community repo.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.community.listApplications"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		applications: {
			type: "object[]",
			required: true,
			children: {
				did: {
					type: "did",
					required: true,
				},
				handle: {
					type: "handle",
					required: true,
				},
				membership: {
					type: "at-uri",
					required: true,
				},
				createdAt: {
					type: "string",
					required: true,
				},
				data: {
					type: "object",
					required: true,
					children: {
						displayName: {
							type: "string",
							required: true,
						},
						avatar: {
							type: "blob",
							required: false,
						},
						banner: {
							type: "blob",
							required: false,
						},
						description: {
							type: "string",
							required: false,
						},
						isBot: {
							type: "boolean",
							required: true,
						},
						onlineState: {
							type: "string",
							required: true,
						},
						status: {
							type: "object",
							required: true,
							children: {
								emoji: {
									type: "string",
									required: false,
								},
								text: {
									type: "string",
									required: true
								}
							}
						},
					}
				}
			}
		},
		dismissedApplications: {
			type: "object[]",
			required: true,
			children: {
				did: {
					type: "did",
					required: true,
				}
			}
		}
	}}
/>

`dismissedApplications` entries share the exact same shape as `applications`.

### `social.colibri.community.dismissApplication`

Hides a pending join application from the active `listApplications` queue without resolving it. Tracked only in AppView storage. Requires `approval.manage`.

Broadcasts `application_event { event: "dismiss", community, membership, did }`.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.dismissApplication"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		did: {
			type: "did",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true
		},
		community: {
			type: "at-uri",
			required: true
		}
	}}
/>

### `social.colibri.community.undismissApplication`

The inverse of `dismissApplication`. Requires `approval.manage`.

Broadcasts `application_event { event: "undismiss", community, membership, did }`.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.undismissApplication"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		did: {
			type: "did",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true
		},
		community: {
			type: "at-uri",
			required: true
		}
	}}
/>

### `social.colibri.category.create`

Creates a new category in a community and appends it to the community's `categoryOrder`. Requires `category.create` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.category.create"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		name: {
			type: "string",
			required: true
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `category_event { event: "upsert", uri, community, name, channelOrder: [] }` and `community_event { event: "upsert", uri, categoryOrder }`.

### `social.colibri.category.update`

Updates a category's name. Requires `category.update` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.category.update"
	authenticated
	queryParams={{
		category: {
			type: "at-uri",
			required: true
		},
		name: {
			type: "string",
			required: false
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `category_event { event: "upsert", uri, name }`.

### `social.colibri.category.delete`

Deletes a category and removes it from the community's `categoryOrder`. Requires `category.delete` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.category.delete"
	authenticated
	queryParams={{
		category: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `category_event { event: "delete", uri }` and `community_event { event: "upsert", uri, categoryOrder }`.

### `social.colibri.channel.create`

Creates a new channel inside a category and appends it to the category's `channelOrder`. Requires `channel.create` permission.

`allowedRoles` and `allowedMembers` optionally seed the channel's post allow-list at creation, letting a channel be born restricted (see [`channel.update`](#socialcolibrichannelupdate) for the allow-list semantics). Both are provided as **repeated** query-string values. The same hierarchy guard as `channel.update` applies to every seeded entry: a non-admin creator may only grant access to roles strictly below their own highest role and to members they outrank; the community owner is exempt. A violation returns `Forbidden`.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.channel.create"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		category: {
			type: "at-uri",
			required: true
		},
		name: {
			type: "string",
			required: true
		},
		type: {
			type: "string",
			required: true
		},
		description: {
			type: "string",
			required: false
		},
		allowedRoles: {
			type: "string[] (repeated)",
			required: false
		},
		allowedMembers: {
			type: "string[] (repeated)",
			required: false
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `channel_event { event: "upsert", uri, community, category, name, type, allowedRoles, allowedMembers }` and `category_event { event: "upsert", uri, channelOrder }`.

### `social.colibri.channel.update`

Updates a channel's name, description, category, owner-only flag, and/or post restrictions. Requires `channel.update` permission.

Passing a `category` that differs from the channel's current one **moves** the channel: the channel record's `category` is rewritten, the rkey is removed from the old category's `channelOrder`, and appended to the new one. The destination category must already be indexed, otherwise the request is rejected with `InvalidRequest` before anything is written. Passing the channel's current category is a no-op. To place the channel at a specific index in its new category, follow the move with `social.colibri.community.reorderChannels`.

`allowedRoles` and `allowedMembers` form an allow-list of who may post in the channel: when both are empty, every community member may post (the default). When either is non-empty, only the community owner, members holding one of the listed roles, or the listed member DIDs may post. `ownerOnly: true` takes precedence over both lists and restricts posting to the owner alone.

Because a non-empty `allowedRoles`/`allowedMembers` list cannot be distinguished from "no change requested" using the list itself, clearing a restriction back to "everyone may post" requires the corresponding `clearAllowedRoles`/`clearAllowedMembers` flag, since passing an empty list is a no-op.

Two hierarchy guards apply:

- **`ownerOnly` is owner-only to change.** Holding `channel.update` is not enough. Only the community owner may flip this flag, since it overrides every other restriction (including the editor's own access, if they're not the owner).
- **`allowedRoles`/`allowedMembers` edits are scoped to the editor's hierarchy.** Only the *entries that actually change* (added or removed relative to the channel's current lists) are checked, entries already present that aren't touched are left alone. For each changed role, the editor's highest role position must be strictly greater than that role's position (so an editor can never add or remove their own top role, or anything at or above it). For each changed member, the editor must outrank that member by the same strictly-greater rule applied to roles, computed the same way as the moderation hierarchy check used by `banUser`/`kickUser`. The community owner is exempt from both checks. A violation returns `Forbidden`.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.channel.update"
	authenticated
	queryParams={{
		channel: {
			type: "at-uri",
			required: true
		},
		category: {
			type: "at-uri",
			required: false
		},
		name: {
			type: "string",
			required: false
		},
		description: {
			type: "string",
			required: false
		},
		ownerOnly: {
			type: "boolean",
			required: false
		},
		allowedRoles: {
			type: "string[] (repeated)",
			required: false
		},
		clearAllowedRoles: {
			type: "boolean",
			required: false
		},
		allowedMembers: {
			type: "string[] (repeated)",
			required: false
		},
		clearAllowedMembers: {
			type: "boolean",
			required: false
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `channel_event { event: "upsert", uri, category, name?, description?, ownerOnly?, allowedRoles?, allowedMembers? }`, plus a `category_event { event: "upsert", uri, channelOrder }` for each affected category when the channel moved.

### `social.colibri.channel.delete`

Deletes a channel and removes it from its parent category's `channelOrder`. Requires `channel.delete` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.channel.delete"
	authenticated
	queryParams={{
		channel: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `channel_event { event: "delete", uri }` and `category_event { event: "upsert", uri, channelOrder }`.

### `social.colibri.community.reorderChannels`

Persists a new channel order within a category. `channelOrder` is provided as **repeated** query-string values, each either an AT URI or a bare record key; anything else is rejected with `InvalidRequest`. An empty `channelOrder` is also rejected when the category currently has one, so a dropped query param can't silently wipe the order. Requires `channel.update` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.reorderChannels"
	authenticated
	queryParams={{
		category: {
			type: "at-uri",
			required: true
		},
		channelOrder: {
			type: "string[] (repeated)",
			required: true
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `category_event { event: "upsert", uri, channelOrder }`.

### `social.colibri.community.reorderCategories`

Persists a new category order for the community sidebar. `categoryOrder` is provided as **repeated** query-string values, each either an AT URI or a bare record key; anything else is rejected with `InvalidRequest`. An empty `categoryOrder` is also rejected when the community currently has one. Requires `category.update` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.reorderCategories"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		categoryOrder: {
			type: "string[] (repeated)",
			required: true
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `community_event { event: "upsert", uri, categoryOrder }`.

### `social.colibri.community.kick`

Removes a member from the community by DID. Writes a `kick` moderation record and revokes the member record. Unlike `kickUser`, accepts a DID directly (no handle resolution). Requires `member.kick` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.kick"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		member: {
			type: "did",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true
		},
		handle: {
			type: "handle",
			required: true
		}
	}}
/>

Broadcasts `member_event { event: "leave", community, memberDid }` via the tap firehose once the member record is deleted.

### `social.colibri.community.setMemberRoles`

Replaces a member's full role set. `roles` is provided as **repeated** query-string values (AT-URIs or bare rkeys). Requires `role.manage` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.setMemberRoles"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		member: {
			type: "did",
			required: true
		},
		roles: {
			type: "string[] (repeated)",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true
		},
		roles: {
			type: "string[]",
			required: true
		}
	}}
/>

Broadcasts `member_event { event: "roles_updated", community, member: { did, roles, data... } }` via the tap firehose once the member record is updated.

### `social.colibri.role.create`

Creates a new role in a community. Requires `role.manage` permission. `position` sets the role's hierarchy rank; higher values sit higher. `permissions` is provided as **repeated** query-string values.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.role.create"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		name: {
			type: "string",
			required: true
		},
		position: {
			type: "integer",
			required: true
		},
		permissions: {
			type: "string[] (repeated)",
			required: false
		},
		color: {
			type: "string (hex #rrggbb)",
			required: false
		},
		hoisted: {
			type: "boolean",
			required: false
		},
		mentionable: {
			type: "boolean",
			required: false
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `role_event { event: "upsert", uri, community, name, permissions, position, color?, hoisted?, mentionable? }` via the tap firehose once the role record is written.

### `social.colibri.role.update`

Updates a role's fields. Only supplied fields are changed; omitted fields keep their current values. Supplying a non-empty `permissions` list **replaces** the current permission set. Requires `role.manage` permission. Returns `InvalidRequest` if the role has `protected: true`.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.role.update"
	authenticated
	queryParams={{
		role: {
			type: "at-uri",
			required: true
		},
		name: {
			type: "string",
			required: false
		},
		color: {
			type: "string (hex #rrggbb)",
			required: false
		},
		permissions: {
			type: "string[] (repeated)",
			required: false
		},
		position: {
			type: "integer",
			required: false
		},
		hoisted: {
			type: "boolean",
			required: false
		},
		mentionable: {
			type: "boolean",
			required: false
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `role_event { event: "upsert", uri, community, name, permissions, position, ... }` via the tap firehose once the role record is updated.

### `social.colibri.role.delete`

Deletes a role from a community. Requires `role.manage` permission. Returns `InvalidRequest` if the role has `protected: true` (e.g. the bootstrap Owner role).

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.role.delete"
	authenticated
	queryParams={{
		role: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true
		}
	}}
/>

Broadcasts `role_event { event: "delete", uri }` via the tap firehose once the role record is deleted.

### `social.colibri.community.listBannedUsers`

Lists all banned users for a community, each hydrated into their full profile (handle, Bluesky profile, and Colibri status).

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.community.listBannedUsers"
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		users: {
			type: "object[]",
			required: true,
			children: {
				did: {
					type: "did",
					required: true,
				},
				handle: {
					type: "handle",
					required: true,
				},
				data: {
					type: "object",
					required: true,
					children: {
						displayName: {
							type: "string",
							required: true,
						},
						avatar: {
							type: "blob",
							required: false,
						},
						banner: {
							type: "blob",
							required: false,
						},
						description: {
							type: "string",
							required: false,
						},
						isBot: {
							type: "boolean",
							required: true,
						},
						onlineState: {
							type: "string",
							required: true,
						},
						status: {
							type: "object",
							required: true,
							children: {
								emoji: {
									type: "string",
									required: false,
								},
								text: {
									type: "string",
									required: true
								}
							}
						},
					}
				}
			}
		}
	}}
/>

### `social.colibri.community.listCategories`

Lists all categories in a community.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.community.listCategories"
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		categories: {
			type: "object[]",
			required: true,
			children: {
				uri: {
					type: "at-uri",
					required: true,
				},
				name: {
					type: "string",
					required: true,
				},
				channelOrder: {
					type: "string[]",
					required: true,
				},
			}
		}
	}}
/>

### `social.colibri.community.listChannels`

Lists all channels in a community.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.community.listChannels"
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		channels: {
			type: "object[]",
			required: true,
			children: {
				uri: {
					type: "at-uri",
					required: true,
				},
				name: {
					type: "string",
					required: true,
				},
				type: {
					type: "string",
					required: true,
				},
				category: {
					type: "string",
					required: true,
				},
				description: {
					type: "string",
					required: false,
				},
				ownerOnly: {
					type: "boolean",
					required: false,
				},
				allowedRoles: {
					type: "string[]",
					required: false,
				},
				allowedMembers: {
					type: "string[]",
					required: false,
				},
			}
		}
	}}
/>

### `social.colibri.community.listRoles`

Lists all roles cached for a community.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.community.listRoles"
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		roles: {
			type: "object[]",
			required: true,
			children: {
				uri: {
					type: "at-uri",
					required: true,
				},
				name: {
					type: "string",
					required: true,
				},
				color: {
					type: "string",
					required: false,
				},
				permissions: {
					type: "string[]",
					required: true,
				},
				position: {
					type: "integer",
					required: true,
				},
				hoisted: {
					type: "boolean",
					required: false,
				},
				mentionable: {
					type: "boolean",
					required: false,
				},
				protected: {
					type: "boolean",
					required: false,
				},
				channelOverrides: {
					type: "object[]",
					required: true,
					children: {
						channel: {
							type: "string",
							required: true,
						},
						allow: {
							type: "string[]",
							required: true,
						},
						deny: {
							type: "string[]",
							required: true,
						},
					}
				},
			}
		}
	}}
/>

### `social.colibri.community.listMembers`

Lists all members with their roles, status information and Bluesky profiles.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.community.listMembers"
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		members: {
			type: "object[]",
			required: true,
			children: {
				did: {
					type: "did",
					required: true,
				},
				handle: {
					type: "handle",
					required: true,
				},
				roles: {
					type: "string[]",
					required: true,
				},
				data: {
					type: "object",
					required: true,
					children: {
						displayName: {
							type: "string",
							required: true,
						},
						avatar: {
							type: "blob",
							required: false,
						},
						banner: {
							type: "blob",
							required: false,
						},
						description: {
							type: "string",
							required: false,
						},
						isBot: {
							type: "boolean",
							required: true,
						},
						onlineState: {
							type: "string",
							required: true,
						},
						status: {
							type: "object",
							required: true,
							children: {
								emoji: {
									type: "string",
									required: false,
								},
								text: {
									type: "string",
									required: true
								}
							}
						},
					}
				}
			}
		}
	}}
/>

### `social.colibri.community.getData`

Returns all cached data for a community in a single response: the community record, its categories, channels, roles, and members. Returns `NotFound` if no community record is cached for the given URI.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.community.getData"
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		community: {
			type: "object",
			required: true,
			children: {
				uri: {
					type: "at-uri",
					required: true,
				},
				name: {
					type: "string",
					required: true,
				},
				description: {
					type: "string",
					required: true,
				},
				picture: {
					type: "blob",
					required: false,
				},
				categoryOrder: {
					type: "string[]",
					required: true,
				},
				requiresApprovalToJoin: {
					type: "boolean",
					required: true,
				},
			}
		},
		categories: {
			type: "object[]",
			required: true,
			children: {
				uri: {
					type: "at-uri",
					required: true,
				},
				name: {
					type: "string",
					required: true,
				},
				channelOrder: {
					type: "string[]",
					required: true,
				},
			}
		},
		channels: {
			type: "object[]",
			required: true,
			children: {
				uri: {
					type: "at-uri",
					required: true,
				},
				name: {
					type: "string",
					required: true,
				},
				type: {
					type: "string",
					required: true,
				},
				category: {
					type: "string",
					required: true,
				},
				description: {
					type: "string",
					required: false,
				},
				ownerOnly: {
					type: "boolean",
					required: false,
				},
				allowedRoles: {
					type: "string[]",
					required: false,
				},
				allowedMembers: {
					type: "string[]",
					required: false,
				},
			}
		},
		roles: {
			type: "object[]",
			required: true,
			children: {
				uri: {
					type: "at-uri",
					required: true,
				},
				name: {
					type: "string",
					required: true,
				},
				color: {
					type: "string",
					required: false,
				},
				permissions: {
					type: "string[]",
					required: true,
				},
				position: {
					type: "integer",
					required: true,
				},
				hoisted: {
					type: "boolean",
					required: false,
				},
				mentionable: {
					type: "boolean",
					required: false,
				},
				protected: {
					type: "boolean",
					required: false,
				},
				channelOverrides: {
					type: "object[]",
					required: true,
					children: {
						channel: {
							type: "string",
							required: true,
						},
						allow: {
							type: "string[]",
							required: true,
						},
						deny: {
							type: "string[]",
							required: true,
						},
					}
				},
			}
		},
		members: {
			type: "object[]",
			required: true,
			children: {
				did: {
					type: "did",
					required: true,
				},
				handle: {
					type: "handle",
					required: true,
				},
				roles: {
					type: "string[]",
					required: true,
				},
				data: {
					type: "object",
					required: true,
					children: {
						displayName: {
							type: "string",
							required: true,
						},
						avatar: {
							type: "blob",
							required: false,
						},
						banner: {
							type: "blob",
							required: false,
						},
						description: {
							type: "string",
							required: false,
						},
						isBot: {
							type: "boolean",
							required: true,
						},
						onlineState: {
							type: "string",
							required: true,
						},
						status: {
							type: "object",
							required: true,
							children: {
								emoji: {
									type: "string",
									required: false,
								},
								text: {
									type: "string",
									required: true
								}
							}
						},
					}
				}
			}
		}
	}}
/>

### `social.colibri.community.blockMessage`

Hides a message in a given community. Requires the caller to hold the `message.hide` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.blockMessage"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		message: {
			type: "at-uri",
			required: true
		},
	}}
	response={{
		message: {
			type: "at-uri",
			required: true,
		}
	}}
/>

Broadcasts `message_event { event: "delete", uri: <message> }` via the tap firehose once the moderation record is indexed, so connected clients remove the hidden message without a reload.

### `social.colibri.community.banUser`

Bans a user from a community by writing a `ban` moderation record. Requires the `member.ban` permission. Callers can only ban members ranked strictly below them in the role hierarchy.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.banUser"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		identifier: {
			type: "did | handle",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true,
		},
		handle: {
			type: "handle",
			required: true,
		}
	}}
/>

### `social.colibri.community.unbanUser`

Unbans a user from a community, allowing them to write messages there again. Requires the `member.unban` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.unbanUser"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		identifier: {
			type: "did | handle",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true,
		},
		handle: {
			type: "handle",
			required: true,
		}
	}}
/>

Broadcasts `member_event { event: "leave", community, memberDid }` via the tap firehose once the member record is revoked.

### `social.colibri.community.kickUser`

Kicks a user from a community by writing a `kick` moderation record and revoking their membership. Unlike a ban, the kicked user is not prevented from rejoining. Requires the `member.ban` permission. Callers can only kick members ranked strictly below them in the role hierarchy.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.kickUser"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
		identifier: {
			type: "did | handle",
			required: true
		}
	}}
	response={{
		did: {
			type: "did",
			required: true,
		},
		handle: {
			type: "handle",
			required: true,
		}
	}}
/>

Broadcasts `member_event { event: "leave", community, memberDid }` via the tap firehose once the member record is revoked.

### `social.colibri.community.createInvitation`

Creates an invitation code for the specified community. Requires the `invitation.create` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.createInvitation"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		},
	}}
	response={{
		code: {
			type: "string",
			required: true,
		},
		community: {
			type: "at-uri",
			required: true,
		},
		createdBy: {
			type: "did",
			required: true
		},
		active: {
			type: "boolean",
			required: true
		}
	}}
/>

### `social.colibri.community.getInvitation`

Get information about a given invitation code. Does not require authentication.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.community.getInvitation"
	queryParams={{
		code: {
			type: "string",
			required: true
		},
	}}
	response={{
		code: {
			type: "string",
			required: true,
		},
		community: {
			type: "at-uri",
			required: true,
		},
		createdBy: {
			type: "did",
			required: true
		},
		active: {
			type: "boolean",
			required: true
		}
	}}
/>

### `social.colibri.community.listInvitations`

List all invitations for the specified community. Requires the `invitation.create` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.listInvitations"
	authenticated
	queryParams={{
		uri: {
			type: "at-uri",
			required: true
		},
	}}
	response={{
		codes: {
			type: "object[]",
			required: true,
			children: {
				code: {
					type: "string",
					required: true,
				},
				community: {
					type: "at-uri",
					required: true,
				},
				createdBy: {
					type: "did",
					required: true
				},
				active: {
					type: "boolean",
					required: true
				}
			}
		}
	}}
/>

### `social.colibri.community.deleteInvitation`

Deactivates an invitation code for the specified community. Requires the `invitation.delete` permission.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.community.deleteInvitation"
	authenticated
	queryParams={{
		uri: {
			type: "at-uri",
			required: true
		},
		code: {
			type: "string",
			required: true
		},
	}}
	response={{
		code: {
			type: "string",
			required: true,
		}
	}}
/>

### `social.colibri.channel.listMessages`

Get a paginated message history for the specified channel, newest messages first. Banned users' messages are filtered out, as are messages that have been hidden by a `hideMessage` moderation action (and not later un-hidden). Hidden messages are excluded from both the top-level list and any embedded `parent`. Set `all` to `true` to include hidden messages in the response (e.g. for moderation views); it defaults to `false`. The `createdAt` field reflects when the message was authored (taken directly from the AT Protocol record), not when the AppView indexed it.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.channel.listMessages"
	queryParams={{
		channel: {
			type: "at-uri",
			required: true
		},
		limit: {
			type: "integer",
			required: false
		},
		cursor: {
			type: "string",
			required: false
		},
		all: {
			type: "boolean",
			required: false
		},
	}}
	response={{
		cursor: {
			type: "string",
			required: false,
		},
		messages: {
			type: "object[]",
			required: true,
			children: {
				uri: {
					type: "at-uri",
					required: true,
				},
				text: {
					type: "string",
					required: true,
				},
				facets: {
					type: "Facet[]",
					required: true,
				},
				channel: {
					type: "at-uri",
					required: true,
				},
				community: {
					type: "at-uri",
					required: true,
				},
				author: {
					type: "object",
					required: true,
					children: {
						did: {
							type: "did",
							required: true,
						},
						handle: {
							type: "handle",
							required: true,
						},
						data: {
							type: "object",
							required: true,
							children: {
								displayName: {
									type: "string",
									required: true,
								},
								avatar: {
									type: "blob",
									required: false,
								},
								banner: {
									type: "blob",
									required: false,
								},
								description: {
									type: "string",
									required: false,
								},
								isBot: {
									type: "boolean",
									required: true,
								},
								onlineState: {
									type: "string",
									required: true,
								},
								status: {
									type: "object",
									required: true,
									children: {
										emoji: {
											type: "string",
											required: false,
										},
										text: {
											type: "string",
											required: true,
										},
									}
								},
							}
						},
					}
				},
				parent: {
					type: "object",
					required: false,
					children: {
						uri: {
							type: "at-uri",
							required: true,
						},
						text: {
							type: "string",
							required: true,
						},
						facets: {
							type: "Facet[]",
							required: true,
						},
						channel: {
							type: "at-uri",
							required: true,
						},
						community: {
							type: "at-uri",
							required: true,
						},
						author: {
							type: "object",
							required: true,
							children: {
								did: {
									type: "did",
									required: true,
								},
								handle: {
									type: "handle",
									required: true,
								},
								data: {
									type: "object",
									required: true,
									children: {
										displayName: {
											type: "string",
											required: true,
										},
										avatar: {
											type: "blob",
											required: false,
										},
										banner: {
											type: "blob",
											required: false,
										},
										description: {
											type: "string",
											required: false,
										},
										isBot: {
											type: "boolean",
											required: true,
										},
										onlineState: {
											type: "string",
											required: true,
										},
										status: {
											type: "object",
											required: true,
											children: {
												emoji: {
													type: "string",
													required: false,
												},
												text: {
													type: "string",
													required: true,
												},
											}
										},
									}
								},
							}
						},
						attachments: {
							type: "object[]",
							required: true,
							children: {
								blob: {
									type: "blob",
									required: true,
								},
								name: {
									type: "string",
									required: false,
								},
							}
						},
						reactions: {
							type: "object[]",
							required: true,
							children: {
								emoji: {
									type: "string",
									required: true,
								},
								count: {
									type: "integer",
									required: true,
								},
								reactorDids: {
									type: "string[]",
									required: true,
								},
							}
						},
						createdAt: {
							type: "datetime",
							required: true,
						},
						edited: {
							type: "boolean",
							required: true,
						},
					}
				},
				attachments: {
					type: "object[]",
					required: true,
					children: {
						blob: {
							type: "blob",
							required: true,
						},
						name: {
							type: "string",
							required: false,
						},
					}
				},
				reactions: {
					type: "object[]",
					required: true,
					children: {
						emoji: {
							type: "string",
							required: true,
						},
						count: {
							type: "integer",
							required: true,
						},
						reactorDids: {
							type: "string[]",
							required: true,
						},
					}
				},
				createdAt: {
					type: "datetime",
					required: true,
				},
				edited: {
					type: "boolean",
					required: true,
				},
			}
		}
	}}
/>

### `social.colibri.channel.getReadCursor`

Get the read cursor for the current user and specified channel.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.channel.getReadCursor"
	authenticated
	queryParams={{
		channel: {
			type: "at-uri",
			required: true
		},
	}}
	response={{
		uri: {
			type: "at-uri",
			required: true,
		},
		cursor: {
			type: "string",
			required: true,
		},
		channel: {
			type: "at-uri",
			required: true,
		}
	}}
/>

### `social.colibri.channel.listUnreadStatus`

Returns per-channel unread status for the authenticated user across every channel in a community.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.channel.listUnreadStatus"
	authenticated
	queryParams={{
		community: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		channels: {
			type: "object[]",
			required: true,
			children: {
				channelUri: {
					type: "at-uri",
					required: true,
				},
				hasUnreadMessages: {
					type: "boolean",
					required: true,
				},
				unreadPingCount: {
					type: "integer",
					required: true,
				},
			}
		}
	}}
/>

### `social.colibri.channel.listReactions`

Get all reactions for a specified message, grouped by emoji with reactor DIDs.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.channel.listReactions"
	queryParams={{
		message: {
			type: "at-uri",
			required: true
		},
	}}
	response={{
		reactions: {
			type: "object[]",
			required: true,
			children: {
				emoji: {
					type: "string",
					required: true,
				},
				count: {
					type: "integer",
					required: true,
				},
				reactorDids: {
					type: "string[]",
					required: true,
				},
			}
		}
	}}
/>

### `social.colibri.embed.getMetadata`

Fetches a URL server-side and returns its OpenGraph/Twitter-card metadata, so the client's IP is never exposed to the target site. Requires authentication to keep the AppView from becoming a public open URL-fetch proxy. Results are cached, including empty ones, to avoid re-fetching pages that carry no embed tags. The outbound request is guarded against SSRF (private, loopback, and link-local addresses are refused). `largeImage` reflects whether the preview should render large or as a small thumbnail.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.embed.getMetadata"
	authenticated
	queryParams={{
		uri: {
			type: "uri",
			required: true
		}
	}}
	response={{
		title: {
			type: "string",
			required: false,
		},
		description: {
			type: "string",
			required: false,
		},
		siteName: {
			type: "string",
			required: false,
		},
		themeColor: {
			type: "string",
			required: false,
		},
		image: {
			type: "object[]",
			required: false,
			children: {
				url: {
					type: "uri",
					required: true,
				},
				alt: {
					type: "string",
					required: false,
				}
			}
		},
		largeImage: {
			type: "boolean",
			required: false,
		}
	}}
/>

### `social.colibri.embed.getImage`

Proxies a remote embed preview image through the AppView so the client's IP is never exposed to the image host. Unauthenticated, like [`com.atproto.sync.getBlob`](#comatprotosyncgetblob), because it is loaded directly via an `<img src>`, but constrained by the same SSRF guard and an `image/*` content-type requirement: anything that isn't an image is rejected.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.embed.getImage"
	queryParams={{
		url: {
			type: "uri",
			required: true
		}
	}}
	response={{
		image: {
			type: "bytes",
			required: true
		}
	}}
/>

### `social.colibri.notification.listNotifications`

Returns a paginated list of notifications for the authenticated user, newest first. Each notification embeds the underlying message body so clients can render the notification without a follow-up fetch. `message` is omitted if the underlying message has been removed from the AppView cache.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.notification.listNotifications"
	authenticated
	queryParams={{
		limit: {
			type: "integer",
			required: false
		},
		cursor: {
			type: "string",
			required: false
		}
	}}
	response={{
		cursor: {
			type: "string",
			required: false
		},
		notifications: {
			type: "object[]",
			required: true,
			children: {
				id: {
					type: "integer",
					required: true
				},
				recipientDid: {
					type: "did",
					required: true
				},
				kind: {
					type: "string",
					required: true
				},
				messageUri: {
					type: "at-uri",
					required: true
				},
				authorDid: {
					type: "did",
					required: true
				},
				channelUri: {
					type: "at-uri",
					required: true
				},
				indexedAt: {
					type: "datetime",
					required: true
				},
				seenAt: {
					type: "datetime",
					required: false
				},
				mentionRoleName: {
					type: "string",
					required: false
				},
				message: {
					type: "object",
					required: false,
					children: {
						text: {
							type: "string",
							required: true
						},
						facets: {
							type: "Facet[]",
							required: true
						},
						createdAt: {
							type: "datetime",
							required: true
						},
						parent: {
							type: "record-key",
							required: false
						},
						attachments: {
							type: "object[]",
							required: true,
							children: {
								blob: {
									type: "blob",
									required: true,
								},
								name: {
									type: "string",
									required: false,
								},
							}
						},
						edited: {
							type: "boolean",
							required: false
						}
					}
				}
			}
		}
	}}
/>

The `kind` value is one of `mention` or `reply`.

Mentions and replies are deduplicated per (recipient, message, kind), so a single message that both mentions and replies to the recipient produces two notification rows.

#### Role mention expansion

A message facet of type `social.colibri.richtext.facet#role` (see [`#role`](/docs/specification/facets#role)) is expanded at index time into one `mention`-kind notification per member holding the role. Because expansion happens where the message is indexed, not where it is written, the client cannot forge a ping.

For each role facet the AppView:

1. Ignores the facet if the role URI's authority doesn't match the message's community (no cross-community pings).
2. Loads the role record and checks its `mentionable` flag. If set, the role is pingable by anyone.
3. If not marked mentionable, expands the role only when the message **author** holds the `mention.roles` permission.
4. Notifies every member whose `roles` include the role, skipping the author. These share the `(recipient, message, kind)` dedup with direct mentions, so a member both `@`-mentioned and role-mentioned receives a single row.

Delivery (live WebSocket and Web Push, including per-recipient DND/mute filtering) is unchanged.

Each role-triggered notification is tagged with the display name of the role that produced it, captured at index time, so clients can render "mentioned via @&lt;role&gt;". This surfaces as `mentionRoleName` on the [`listNotifications`](#socialcolibrinotificationlistnotifications) view and the [`notification_event`](#notification_event) payload, and as the Web Push title (`Mentioned via @&lt;role&gt;`). When a member holds several of the mentioned roles they are attributed to the first one in the message's facet order. A member who is both directly mentioned and role-mentioned keeps the direct mention with no role tag.

### `social.colibri.notification.getUnreadCount`

Returns the count of unseen notifications for the authenticated user.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.notification.getUnreadCount"
	authenticated
	response={{
		count: {
			type: "integer",
			required: true
		}
	}}
/>

### `social.colibri.notification.updateSeen`

Marks every unseen notification for the authenticated user whose `indexedAt` is at or before `seenAt` as seen. If `seenAt` is omitted, the AppView uses its current clock for both the cutoff and the stamped `seenAt`.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.notification.updateSeen"
	authenticated
	queryParams={{
		seenAt: {
			type: "datetime",
			required: false
		}
	}}
	response={{
		updated: {
			type: "integer",
			required: true
		}
	}}
/>

### `social.colibri.notification.getUnseen`

Returns the authenticated user's unseen notifications within a single channel. Clients use this when a channel opens to learn which messages still owe a ping, so each one can be cleared (via [`updateSeenForMessage`](#socialcolibrinotificationupdateseenformessage)) as its message scrolls into view.

<XRPCEndpointCard
	method="GET"
	nsid="social.colibri.notification.getUnseen"
	authenticated
	queryParams={{
		channel: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		notifications: {
			type: "object[]",
			required: true,
			children: {
				id: {
					type: "integer",
					required: true,
				},
				messageUri: {
					type: "at-uri",
					required: true,
				},
				indexedAt: {
					type: "datetime",
					required: true,
				},
			}
		}
	}}
/>

### `social.colibri.notification.updateSeenForMessage`

Marks the authenticated user's unseen notifications for a single message as seen. When at least one row is cleared, the AppView broadcasts a [`seen_event`](#seen_event) with `event: "message_seen"` to the user's other connected clients so their unread badges update live.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.notification.updateSeenForMessage"
	authenticated
	queryParams={{
		message: {
			type: "at-uri",
			required: true
		}
	}}
	response={{
		updated: {
			type: "integer",
			required: true
		}
	}}
/>

### `social.colibri.notification.registerPush`

Registers a Web Push subscription for the authenticated user so the AppView can deliver background push notifications when the app is closed.

:::note[Service routing]
This endpoint and [`unregisterPush`](#socialcolibrinotificationunregisterpush) belong to the notification service: proxy them with `atproto-proxy: did:web:api.colibri.social#colibri_notif` rather than `#colibri_appview`.
:::

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.notification.registerPush"
	authenticated
	body={{
		platform: {
			type: "string",
			required: true
		},
		endpoint: {
			type: "string",
			required: true
		},
		keys: {
			type: "object",
			required: true,
			children: {
				p256dh: {
					type: "string",
					required: true
				},
				auth: {
					type: "string",
					required: true
				}
			}
		}
	}}
	response={{
		registered: {
			type: "boolean",
			required: true
		}
	}}
/>

### `social.colibri.notification.unregisterPush`

Drops a previously registered Web Push subscription for the authenticated user, identified by its `endpoint`. Called when the user disables notifications or the browser rotates/expires the subscription.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.notification.unregisterPush"
	authenticated
	body={{
		endpoint: {
			type: "string",
			required: true
		}
	}}
	response={{
		unregistered: {
			type: "boolean",
			required: true
		}
	}}
/>

#### `notification_event`

Notification events are emitted over [`social.colibri.sync.subscribeEvents`](#socialcolibrisyncsubscribeevents) immediately after a notification is indexed. The payload mirrors a single entry of [`listNotifications`](#socialcolibrinotificationlistnotifications) and always carries the message body.

```ts title="data"
{
	id: integer;
	kind: 'mention' | 'reply';
	messageUri: string;
	authorDid: string;
	channelUri: string;
	indexedAt: string;
	mentionRoleName?: string; // set when a role mention triggered this notification
	message: {
		text: string;
		facets: facet[];
		createdAt: string;
		parent?: string;
		attachments: { blob: blob; name?: string; }[];
		edited?: boolean;
	};
}
```

##### Web Push delivery

Alongside each `notification_event` broadcast over the WebSocket, the AppView also sends a [VAPID Web Push](https://datatracker.ietf.org/doc/html/rfc8030) message to every Web Push subscription the recipient has registered via [`registerPush`](#socialcolibrinotificationregisterpush).

The push payload is JSON shaped for the Service Worker that renders it:

```ts title="push payload"
{
	title: string;        // "New mention" | "New reply" | "Mentioned via @<role>"
	body: string;         // the message text
	tag?: string;         // the message URI (collapses duplicates)
	icon?: string;
	data: {
		channelUri: string; // required
		messageUri: string;
	};
}
```

The AppView owns the VAPID keypair and exposes the public key to clients out of band (as the `PUBLIC_VAPID_KEY` build-time environment variable). Subscriptions that the push service reports as gone (HTTP `404`/`410`) are pruned automatically.

#### `seen_event`

Emitted over [`social.colibri.sync.subscribeEvents`](#socialcolibrisyncsubscribeevents) to keep unread state in sync across a user's devices. Unlike most events, a `seen_event` is delivered only to the originating user's own connected clients (filtered by DID).

```ts title="data"
{
	event: 'channel_read' | 'message_seen';
	channelUri: string;
	messageUri?: string; // present on message_seen
	cleared?: number;    // present on message_seen
}
```

## Humming

:::caution[Experimental]
Humming is **enabled by default**. AppViews can opt out out by returning  a `NotEnabled` error on the endpoint below and opening no outbound connections. In the reference AppView, this can be achieved by starting the AppView with `HUMMING_ENABLED=false`. Cross-instance behaviour has not yet been hardened by a large federated deployment, so treat it as experimental.
:::

Colibri is built to be self-hostable, so it's possible that a community's members are spread across several AppViews. On-protocol data reaches every AppView through the tap firehose. **Off-protocol** signals, like online status, typing, voice presence, never touch a repo, so without Humming they are contained within the AppView that produced them. A user on one instance can't see the presence of a community member on another.

**Humming** bridges that gap. A **Hum** is a single off-protocol event (a status change, a typing start, a voice join/leave) that one AppView relays to another so it can be delivered to that instance's clients over its ordinary [`social.colibri.sync.subscribeEvents`](#socialcolibrisyncsubscribeevents) stream. Humming carries **only** these ephemeral signals. It cannot carry a message, a membership, or any other on-protocol data.

### Topology: per-community hub

Every community already has exactly one AppView that administers it, the holder of its `community_credentials`, the only party that can write to the community repo. That AppView is the community's **presence hub**. It is discovered from the community record itself: `social.colibri.community` carries an `appview` field (a `did:web`) naming the home AppView.

Presence flows through the hub in a **star** topology, not a gossip mesh:

1. A **leaf** (any AppView with a local, online member of the community) sends the member's Hums to the community's hub via [`sendHum`](#socialcolibrisyncsendhum).
2. The **hub** injects the Hum for its own local clients and fans it out to every leaf subscribed via [`subscribeHums`](#socialcolibrisyncsubscribehums).
3. **Leaves never re-forward.** A Hum received over `subscribeHums` is delivered to local clients only.

A Hum therefore makes **at most two hops** (leaf → hub → leaf), and an instance only ever sees traffic for communities its own users belong to. There is no whole-network flood.

### Trust model

A Hum is only acted on if it passes every check below. The design goal is that a forged or abusive Hum can, at worst, cause a cosmetic presence blip for a user who already opted in.

1. **Off-protocol data only.** The wire type for a Hum's event is a closed union of `userEvent`, `typingEvent`, and `voicePresenceEvent`. An on-protocol payload will be ignored.
2. **The peer AppView is authenticated.** Every Hum carries an [inter-service auth](https://atproto.com/specs/xrpc#inter-service-authentication-jwt) JWT signed by the sending AppView's key (`iss` = its `did:web`, `aud` = the receiver's `did:web`, `lxm` = the method NSID). This is a peer-to-peer AppView token, **not** the user OAuth token used by the rest of the API.
3. **The origin must be an AppView.** The verified issuer must be a `did:web`. This stops a user from minting a service-auth token under their own `did:plc` and originating presence for themselves.
4. **The origin must be the subject's declared presence service.** A Hum about a user is dropped unless its `origin` equals that user's `presenceService` (see [Opting in](#opting-in)). A valid signature proves *who sent* the Hum, this check proves *who they may speak for*. A missing `presenceService` means the user hasn't opted in, so the Hum is dropped.
5. **The subject must be a member.** The subject must hold a `social.colibri.member` record on the target community, so a peer can't inject a user into a community they never joined.
6. **Channels must belong to the community.** Any channel a typing or voice event references must be an `at-uri` whose authority is the target community.

Finally, the receiver re-derives the subject's identity from its own view of that user's repo and attributes the injected event to the envelope's `subject`. The peer-supplied identity fields inside the event payload (display name, handle, the inner `did`) are discarded and only the ephemeral status/typing/voice signal is taken from the Hum. This closes any attempt to attribute an event to a different user.

### Opting in

Cross-instance presence is **opt-in per user**. A user opts in by publishing a `presenceService` field (a `did:web`) on their `social.colibri.actor.profile`, naming the AppView allowed to speak for their presence. The reference Colibri client keeps this in sync with the AppView the user actually connects to (the one chosen in the settings picker) and exposes a **"Share presence across AppViews"** toggle, on by default.

Turning the toggle off removes the `presenceService` key from the profile. A missing key is the signal for "opted out": every AppView drops Hums about that user, so their presence, typing, and voice never cross an instance boundary. Local, same-AppView presence is unaffected.

### The Hum envelope

Both endpoints exchange the same envelope:

```ts title="humEnvelope"
{
	origin: string;      // did:web of the AppView that produced the Hum
	id: string;          // unique per origin, used for dedup across relay paths
	ttl: number;         // remaining hops (0..8), starts at 1, hub decrements to 0
	subject: string;     // did of the user the event is about
	community: string;   // at-uri of the community the event is scoped to
	event: UserEvent | TypingEvent | VoicePresenceEvent;
}
```

`userEvent` and `typingEvent` are the same shapes emitted over [`subscribeEvents`](#socialcolibrisyncsubscribeevents). `voicePresenceEvent` describes a voice channel join/leave:

```ts title="voicePresenceEvent"
{
	event: 'join' | 'leave';
	channel: string; // at-uri of the voice channel
	did: string;     // subject (re-derived by the receiver)
}
```

### `social.colibri.sync.sendHum`

Informs this AppView, acting as a community's hub, of an off-protocol event that occurred for one of the calling AppView's users.

<XRPCEndpointCard
	method="POST"
	nsid="social.colibri.sync.sendHum"
	body={{
		origin: {
			type: "did",
			required: true
		},
		id: {
			type: "string",
			required: true
		},
		ttl: {
			type: "integer",
			required: true
		},
		subject: {
			type: "did",
			required: true
		},
		community: {
			type: "at-uri",
			required: true
		},
		event: {
			type: "object",
			required: true
		}
	}}
/>

#### Authentication

`sendHum` is authenticated with a peer-AppView **inter-service auth** JWT, not the user OAuth token used elsewhere. The token is supplied as a bearer token (`Authorization: Bearer <jwt>`), with `aud` set to the receiving AppView's `did:web` and `lxm` set to `social.colibri.sync.sendHum`. The receiver resolves the issuer's DID document, verifies the signature, and treats `iss` as the peer AppView identity, which must equal the envelope's `origin`.

The receiver then runs the [trust model](#trust-model) checks, injects the event locally (presence and voice as `Community`-scoped events on `subscribeEvents`, typing as a client-to-client typing event), and, if it is a hub, while `ttl > 0`, decrements `ttl` and republishes the Hum to its `subscribeHums` subscribers.

Errors: `AuthRequired` (missing/invalid token), `Forbidden` (any trust-model check failed), `RateLimited` (peer exceeded its per-peer budget), `UnsupportedEvent` (payload was not an off-protocol event), `NotEnabled` (Humming disabled on this AppView).

### `social.colibri.sync.subscribeHums`

A peer AppView opens this WebSocket to receive, in real time, the Hums this AppView relays in its hub role.

<XRPCEndpointCard
	method="WS"
	nsid="social.colibri.sync.subscribeHums"
/>

#### Authentication

Like [`subscribeEvents`](#socialcolibrisyncsubscribeevents), a browser-incompatible header can't be set on a `WebSocket`, so the peer's inter-service auth JWT (`aud` = this AppView, `lxm` = `social.colibri.sync.subscribeHums`) is offered through the subprotocol list, after the same `colibri.auth.bearer` sentinel:

```js
new WebSocket(url, ["colibri.auth.bearer", token]);
```

The sentinel is echoed back on success, the token is not.

#### Need-to-know egress

A valid token authenticates *who* is connecting, not *what* they may see. Streaming every relayed Hum to any authenticated peer would leak the presence of every user in every community on the hub. Instead, the hub only forwards Hums for a community when that community has a member who declared the connecting peer as their `presenceService`. This authorized set is recomputed periodically as memberships and `presenceService` choices change.

A peer may additionally narrow what it receives by declaring the communities it cares about as repeated `communities` query parameters (`?communities=<did>&communities=<did>`). This is intersected with the authorized set: it can only shrink the stream, never widen it. Omitting it streams everything the peer is authorized for.

:::note
`subscribeHums` is egress-only: inbound frames are ignored except the close signal. Peers push Hums with [`sendHum`](#socialcolibrisyncsendhum), never here.
:::

### Propagation bounds & abuse controls

The star topology bounds propagation structurally (two hops, no re-forwarding), and several additional guards protect self-hosted instances:

- **`ttl`** starts at `1` and the hub decrements it to `0` on rebroadcast; a bound that, combined with leaves never re-forwarding, prevents loops.
- **Dedup.** Each AppView keeps a bounded set of recently-seen envelope `id`s and drops duplicates that arrive over more than one relay path, along with any Hum it originated itself.
- **`HUM_MAX_PEERS`** caps the number of distinct remote hubs a leaf will dial (default `32`). Beyond the cap, extra hubs are skipped.
- **`HUM_MAX_SUBSCRIBERS`** caps concurrent inbound `subscribeHums` streams a hub will hold (default `256`). Further connections are rejected with `TooManySubscribers`.
- **Per-peer rate limit** on `sendHum` (`HUM_RATE_LIMIT`, default `240`/minute per peer). Presence and typing are inherently low-rate, so a peer exceeding the budget is `RateLimited`.
