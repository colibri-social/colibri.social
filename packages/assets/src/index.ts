// Browser-safe entry: root URLs for shared static assets. The files themselves
// live in this package's files/ dir and are provisioned to the site root by the
// host (see ./node.js + the consuming app's static-copy config).
export const logoUrl = "/logo.png";
export const userPlaceholderUrl = "/user-placeholder.png";
