# @colibri-social/lib

Framework-agnostic shared code used by the client and website.

## Getting started

Import it from another workspace package:

```ts
import {} from /* ... */ "@colibri-social/lib";
```

Changes here are picked up automatically by the client and website dev servers. A type-only build is available via `pnpm build` (uses the TypeScript native preview compiler).
