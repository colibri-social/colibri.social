{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capabilities (window-state plugin is compiled out on mobile).",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": ["window-state:default"]
}
