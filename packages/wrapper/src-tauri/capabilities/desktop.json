{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capabilities (updater + window-state plugins are compiled out on mobile).",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": ["updater:default", "window-state:default"]
}
